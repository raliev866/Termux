













## 📋 **ПОЛНЫЙ СПИСОК КОМАНД, КОТОРЫЕ МЫ СОЗДАЛИ**



### Алиасы (короткие команды):

```bash
alias
```

Должен показать:
- `alias индекс='~/create_double_indexes.sh'`
- `alias make-indexes='~/create_all_indexes.sh'`
- `alias gitsync='~/git-sync.sh'`
- И другие, если добавляли

### Как посмотреть все скрипты в домашней папке:

```bash
ls -la ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления 2>/dev/null
```

### Как посмотреть все команды в системной папке:

```bash
ls -la /data/data/com.termux/files/usr/bin/ | grep -E "(док|оглавления|все|обзор)"
```

### Если хочешь увидеть содержимое конкретного скрипта:

```bash
cat ~/док
# или
cat ~/оглавления
# или
cat ~/все
```

## 📌 **САМЫЕ ПОЛЕЗНЫЕ КОМАНДЫ:**

1. **`оглавления`** — полный обзор всех папок (самая мощная)
2. **`док`** — конвертация MD в DOCX
3. **`~/git-sync.sh`** — синхронизация с GitHub
4. **`индекс`** — создание индекс-файлов (если алиас настроен)




.