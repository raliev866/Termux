








## 📋 **1. ПРОСТОЙ СПИСОК КОМАНД (УНИКАЛЬНЫЕ)**


# Основные команды (доступны из любого места):


*  оглавления

*  док

*  индекс

*  проект

*  html

*  pdf

*  word

*  бэкап

*  книга

*  мд

*  мдтекст

*  суперформат

*  формат

# Скрипты из домашней папки (~/)
~/все
~/обзор
~/оглавление
~/док
~/md2docx.sh
~/git-sync.sh
~/create_all_indexes.sh
~/create_double_indexes.sh
~/create_full_catalog.sh
~/create_indexes_inside_only.sh
~/create_nice_links.sh
~/create_working_links.sh
~/format.sh
~/full_backup.sh
~/html.sh
~/make_books.sh
~/make_pdf.sh
~/md_text_extract.sh
~/superformat.sh
~/sync.sh





## 📁 **2. СПИСОК С НАЗВАНИЯМИ ПАПОК**

| Команда | Папка/Проект |
|---------|--------------|
| `оглавления` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `док` | `/storage/emulated/0/Documents/08_Из_md_в_docx_док` |
| `индекс` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `проект` | `cd "/sdcard/Documents/01. GitHub. .me, .txt, (.HTML)/01_ПРОЕКТ_КНИГА_ИИ_.md"` |
| `html` | Текущая папка (./) |
| `pdf` | `/storage/emulated/0/Documents/09_Из_md_docx_HTML_в_PDF_pdf` |
| `word` | Текущая папка (./) |
| `бэкап` | Текущая папка (./) |
| `книга` | Текущая папка (./) |
| `мд` | Текущая папка (./) |
| `мдтекст` | Текущая папка (./) |
| `суперформат` | Домашняя папка (~/) |
| `формат` | Домашняя папка (~/) |
| `~/все` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `~/обзор` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `~/оглавление` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `~/create_*` | `/storage/emulated/0/Documents/01_GitHub_DeepSeekProject` |
| `~/format.sh` | Домашняя папка (~/) |
| `~/full_backup.sh` | Вся система |
| `~/html.sh` | Текущая папка |
| `~/make_pdf.sh` | `/storage/emulated/0/Documents/09_Из_md_docx_HTML_в_PDF_pdf` |

## 📝 **3. СПИСОК С ОПИСАНИЕМ РАБОТ**

| Команда | Описание |
|---------|----------|
| **`оглавления`** | Универсальный скрипт: создаёт FullScan.md/.html (полный обзор) и Оглавление.md/.html (список файлов) для всех папок проекта |
| **`док`** | Конвертирует все .md файлы в .docx, сохраняя полную структуру папок (создаёт папки с припиской " - док") |
| **`индекс`** | Создаёт красивые индекс-файлы со ссылками на все файлы в папках |
| **`проект`** | Быстрый переход в папку проекта с книгой |
| **`html`** | Конвертирует файлы в HTML |
| **`pdf`** | Создаёт PDF из документов |
| **`word`** | Быстрая конвертация всех .md файлов в .docx в текущей папке |
| **`бэкап`** | Создаёт полную резервную копию проекта |
| **`книга`** | Собирает книгу из файлов |
| **`мд`** | Базовая конвертация .md в .docx |
| **`мдтекст`** | Извлекает текст из .md файлов |
| **`суперформат`** | Продвинутое форматирование файлов |
| **`формат`** | Базовое форматирование |
| **`~/все`** | То же, что и `оглавления` (полный обзор + оглавление) |
| **`~/обзор`** | Создаёт полный обзор всех файлов и папок с детальной статистикой |
| **`~/оглавление`** | Создаёт простое оглавление со ссылками на файлы |
| **`~/git-sync.sh`** | Синхронизирует с GitHub (add, commit, push) |
| **`~/create_all_indexes.sh`** | Создаёт индексы для всех корневых папок |
| **`~/create_double_indexes.sh`** | Создаёт индексы в двух форматах (.md и .html) |
| **`~/create_full_catalog.sh`** | Создаёт полный каталог всех файлов |
| **`~/create_indexes_inside_only.sh`** | Создаёт индексы только внутри папок |
| **`~/create_nice_links.sh`** | Создаёт красивые ссылки |
| **`~/create_working_links.sh`** | Создаёт рабочие ссылки на GitHub |
| **`~/full_backup.sh`** | Полный бэкап всей системы |
| **`~/make_pdf.sh`** | Конвертирует в PDF |
| **`~/sync.sh`** | Простая синхронизация |

## 🔧 **СКРИПТ ДЛЯ ПРОВЕРКИ (если нужно уточнить):**



```bash
cat > ~/check_commands.sh << 'EOF'
#!/bin/bash

echo "=== ПРОВЕРКА КОМАНД ==="
for cmd in оглавления док индекс проект html pdf word бэкап книга мд мдтекст суперформат формат; do
    if command -v $cmd &> /dev/null; then
        echo "✅ $cmd — доступна"
        
        # Проверяем, является ли команда алиасом
        alias_output=$(alias $cmd 2>/dev/null)
        if [ -n "$alias_output" ]; then
            alias_target=$(echo "$alias_output" | sed "s/.*='\(.*\)'/\1/")
            echo "   → это алиас к $alias_target"
        fi
    else
        echo "❌ $cmd — не найдена"
    fi
done

echo ""
echo "=== СКРИПТЫ В ~/ ==="
ls -la ~/*.sh ~/все ~/док ~/обзор ~/оглавление 2>/dev/null | awk '{print "   " $9}'

echo ""
echo "=== КОМАНДЫ В /usr/bin/ ==="
ls -la /data/data/com.termux/files/usr/bin/ | grep -E "(док|оглавления|все|обзор)" 2>/dev/null | awk '{print "   " $9}'
EOF

chmod +x ~/check_commands.sh
```

## ✅ **ЗАПУСКАЙ:**

```bash
~/check_commands.sh
```

## 📋 **ИЛИ ПРОСТОЙ ВАРИАНТ (без скрипта):**

```bash
# Посмотреть все алиасы
alias

# Посмотреть все скрипты в домашней папке
ls -la ~/*.sh ~/все ~/док ~/обзор ~/оглавление 2>/dev/null

# Посмотреть команды в системной папке
ls -la /data/data/com.termux/files/usr/bin/ | grep -E "(док|оглавления|все|обзор)"
```











Да, некоторые команды **действительно перестали работать**. Давай разберём по группам:

## ✅ **РАБОТАЮЩИЕ КОМАНДЫ:**

| Команда | Статус | Примечание |
|---------|--------|------------|
| **`оглавления`** | ✅ РАБОТАЕТ | Есть в `/usr/bin/` и как алиас к `~/все` |
| **`док`** | ✅ РАБОТАЕТ | Есть в `/usr/bin/` и как `~/док` |
| **`html`** | ✅ РАБОТАЕТ | Алиас к `./html.sh` |
| **`pdf`** | ✅ РАБОТАЕТ | Алиас к скрипту в папке PDF |
| **`бэкап`** | ✅ РАБОТАЕТ | Алиас к `./full_backup.sh` |
| **`книга`** | ✅ РАБОТАЕТ | Алиас к `./make_books.sh` |
| **`мд`** | ✅ РАБОТАЕТ | Алиас к `./md2docx.sh` |
| **`формат`** | ✅ РАБОТАЕТ | Алиас к `~/format.sh` |

## ❌ **НЕРАБОТАЮЩИЕ КОМАНДЫ:**

| Команда | Статус | Причина |
|---------|--------|---------|
| **`индекс`** | ❌ НЕ РАБОТАЕТ | Алиас есть, но файл `~/create_double_indexes.sh` существует. Возможно, проблема в правах или в том, что алиас не подгрузился |
| **`проект`** | ❌ НЕ РАБОТАЕТ | Алиас ведёт в несуществующую папку (проверь путь) |
| **`word`** | ❌ НЕ РАБОТАЕТ | Алиас есть, но это просто цикл for, который может не находить файлы |
| **`мдтекст`** | ❌ НЕ РАБОТАЕТ | Алиас есть, но файл `./md_text_extract.sh` возможно не там или неисполняемый |
| **`суперформат`** | ❌ НЕ РАБОТАЕТ | Алиас есть, но файл `~/superformat.sh` существует. Возможно, проблема с bash путём |

## 🔧 **ЧТО МОЖНО СДЕЛАТЬ:**

### 1. Перезагрузить алиасы:
```bash
source ~/.bashrc
```

### 2. Проверить конкретные файлы:
```bash
# Проверить индекс
ls -la ~/create_double_indexes.sh
# Должен быть -rwx------ (исполняемый)

# Проверить суперформат
ls -la ~/superformat.sh

# Проверить мдтекст
ls -la ~/md_text_extract.sh
```

### 3. Если файлы есть, но не работают — сделать исполняемыми:
```bash
chmod +x ~/create_double_indexes.sh
chmod +x ~/superformat.sh
chmod +x ~/md_text_extract.sh
```

### 4. Проверить путь для `проект`:
```bash
# Посмотреть, существует ли папка
ls -la "/sdcard/Documents/01. GitHub. .me, .txt, (.HTML)/01_ПРОЕКТ_КНИГА_ИИ_.md"
```

## 📌 **ВЫВОД:**

Большинство команд **работают**, но некоторые требуют проверки:
- `индекс` — файл есть, нужно перезагрузить алиасы
- `проект` — скорее всего неправильный путь
- `word` — это просто цикл, может не находить .md файлы
- `мдтекст` и `суперформат` — нужно проверить наличие файлов

Хочешь, я помогу восстановить неработающие команды?

















