










Понял! Удаляем команду `док`:

## 🔧 **Полное удаление команды `док`**

### 1. Удали скрипт из домашней папки:
```bash
rm -f ~/док
```

### 2. Удали из системной папки (если туда копировал):
```bash
rm -f /data/data/com.termux/files/usr/bin/док
```

### 3. Удали алиас (если добавлял):
```bash
sed -i '/alias док/d' ~/.bashrc
source ~/.bashrc
```

### 4. Проверь, что команда удалена:
```bash
which док
```
Должно вернуть пустоту или "command not found"

## ✅ **Готово! Команда `док` больше не существует.**






























