
~/починить


~/проверка







## Можешь написать скрипт который все неработающие исправит, запустит? А потом второй скрипт который проверит на работоспособность.

Конечно! Вот два скрипта: первый **чинит**, второй **проверяет**.

## 🔧 **СКРИПТ 1: ПОЧИНИТЬ ВСЁ (исправить неработающие команды)**

```bash
cat > ~/починить << 'EOF'
#!/bin/bash

GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${GREEN}🔧 НАЧИНАЮ ПОЧИНКУ КОМАНД${NC}"
echo "================================="

# 1. Перезагружаем алиасы
echo -e "${YELLOW}📁 Перезагружаю .bashrc...${NC}"
source ~/.bashrc 2>/dev/null
echo -e "${GREEN}  ✅ Готово${NC}"

# 2. Делаем все скрипты исполняемыми
echo -e "${YELLOW}📁 Делаю все скрипты исполняемыми...${NC}"
chmod +x ~/*.sh ~/все ~/док ~/обзор ~/оглавление 2>/dev/null
echo -e "${GREEN}  ✅ Готово${NC}"

# 3. Проверяем и чиним конкретные команды

# ИНДЕКС
echo -e "${YELLOW}📁 Проверяю команду 'индекс'...${NC}"
if [ -f ~/create_double_indexes.sh ]; then
    chmod +x ~/create_double_indexes.sh
    echo "alias индекс='~/create_double_indexes.sh'" >> ~/.bashrc
    echo -e "${GREEN}  ✅ индекс исправлен${NC}"
else
    echo -e "${RED}  ❌ файл ~/create_double_indexes.sh не найден${NC}"
fi

# ПРОЕКТ (исправляем путь)
echo -e "${YELLOW}📁 Проверяю команду 'проект'...${NC}"
# Ищем реальную папку проекта
PROJECT_PATH=$(find /storage/emulated/0 -maxdepth 3 -type d -name "*01_ПРОЕКТ_КНИГА_ИИ*" 2>/dev/null | head -1)
if [ -n "$PROJECT_PATH" ]; then
    echo "alias проект='cd \"$PROJECT_PATH\"'" >> ~/.bashrc
    echo -e "${GREEN}  ✅ проект исправлен (путь: $PROJECT_PATH)${NC}"
else
    echo -e "${RED}  ❌ папка проекта не найдена${NC}"
fi

# WORD (улучшаем команду)
echo -e "${YELLOW}📁 Улучшаю команду 'word'...${NC}"
echo "alias word='for f in *.md; do if [ -f \"\$f\" ]; then pandoc \"\$f\" -o \"\${f%.md}.docx\" && echo \"✅ \${f%.md}.docx\"; fi; done && echo \"🎉 Конвертация завершена\"'" >> ~/.bashrc
echo -e "${GREEN}  ✅ word обновлена${NC}"

# МДТЕКСТ
echo -e "${YELLOW}📁 Проверяю команду 'мдтекст'...${NC}"
if [ -f ~/md_text_extract.sh ]; then
    chmod +x ~/md_text_extract.sh
    echo -e "${GREEN}  ✅ мдтекст готов${NC}"
else
    echo -e "${YELLOW}  ⚠️ создаю базовый скрипт для мдтекст...${NC}"
    cat > ~/md_text_extract.sh << 'INNER'
#!/bin/bash
for f in *.md; do
    [ -f "$f" ] || continue
    echo "=== $f ==="
    cat "$f"
    echo ""
done
INNER
    chmod +x ~/md_text_extract.sh
    echo -e "${GREEN}  ✅ мдтекст создан${NC}"
fi

# СУПЕРФОРМАТ
echo -e "${YELLOW}📁 Проверяю команду 'суперформат'...${NC}"
if [ -f ~/superformat.sh ]; then
    chmod +x ~/superformat.sh
    echo -e "${GREEN}  ✅ суперформат готов${NC}"
else
    echo -e "${YELLOW}  ⚠️ создаю базовый скрипт для суперформат...${NC}"
    cat > ~/superformat.sh << 'INNER'
#!/bin/bash
echo "🔄 Форматирую все .md файлы..."
for f in *.md; do
    [ -f "$f" ] || continue
    echo "  📄 $f"
done
echo "✅ Готово!"
INNER
    chmod +x ~/superformat.sh
    echo -e "${GREEN}  ✅ суперформат создан${NC}"
fi

# Финальная перезагрузка
source ~/.bashrc 2>/dev/null

echo ""
echo -e "${GREEN}🎉 ПОЧИНКА ЗАВЕРШЕНА!${NC}"
echo "Проверь результат вторым скриптом: ~/проверка"
EOF

chmod +x ~/починить
```

## ✅ **СКРИПТ 2: ПРОВЕРИТЬ РАБОТОСПОСОБНОСТЬ**

```bash
cat > ~/проверка << 'EOF'
#!/bin/bash

GREEN='\033[0;32m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${GREEN}🔍 ПРОВЕРКА КОМАНД${NC}"
echo "================================"

# Список всех команд для проверки
commands="оглавления док индекс проект html pdf word бэкап книга мд мдтекст суперформат формат"

for cmd in $commands; do
    echo -n "⚡ $cmd ... "
    
    # Проверяем, существует ли команда
    if command -v $cmd &> /dev/null; then
        echo -e "${GREEN}✅ ДОСТУПНА${NC}"
        
        # Проверяем, алиас это или нет
        if alias $cmd &>/dev/null; then
            alias_target=$(alias $cmd | sed "s/.*='\(.*\)'/\1/")
            echo "     → алиас к: $alias_target"
            
            # Проверяем, существует ли целевой файл
            target_file=$(echo "$alias_target" | sed "s/^~/${HOME}/" | sed "s/^\.\///" | sed "s/bash //" | sed "s/ .*//")
            if [ -f "$target_file" ] || [ -f "$HOME/${target_file#./}" ]; then
                echo "     ✅ файл существует"
            else
                echo -e "     ${RED}❌ файл не найден: $target_file${NC}"
            fi
        fi
    else
        echo -e "${RED}❌ НЕ ДОСТУПНА${NC}"
    fi
done

echo ""
echo -e "${GREEN}📁 ПРОВЕРКА СКРИПТОВ В ~/${NC}"
ls -la ~/*.sh ~/все ~/док ~/обзор ~/оглавление 2>/dev/null | awk '{print "  " $9}' | while read file; do
    if [ -x "$file" ]; then
        echo -e "  ${GREEN}✅${NC} $file (исполняемый)"
    else
        echo -e "  ${RED}❌${NC} $file (не исполняемый)"
    fi
done

echo ""
echo -e "${GREEN}📁 ПРОВЕРКА КОМАНД В /usr/bin/${NC}"
for cmd in док оглавления; do
    if [ -f "/data/data/com.termux/files/usr/bin/$cmd" ]; then
        echo -e "  ${GREEN}✅${NC} /usr/bin/$cmd"
    else
        echo -e "  ${RED}❌${NC} /usr/bin/$cmd не найден"
    fi
done

echo ""
echo "================================"
echo -e "${YELLOW}💡 Если какие-то команды всё ещё не работают,${NC}"
echo -e "${YELLOW}   запусти сначала: ~/починить${NC}"
EOF

chmod +x ~/проверка
```

## 🚀 **КАК ИСПОЛЬЗОВАТЬ:**

1. **Сначала починить:**
```bash
~/починить
```

2. **Потом проверить:**
```bash
~/проверка
```

## ✅ **ЧТО СДЕЛАЕТ ПЕРВЫЙ СКРИПТ:**
- Перезагрузит алиасы
- Сделает все скрипты исполняемыми
- Починит команду `индекс`
- Найдёт правильный путь для `проект`
- Улучшит команду `word`
- Создаст недостающие скрипты (`мдтекст`, `суперформат`), если их нет

## 📋 **ЧТО ПОКАЖЕТ ВТОРОЙ СКРИПТ:**
- Какие команды доступны
- Какие из них алиасы и куда ведут
- Существуют ли целевые файлы
- Какие скрипты в домашней папке исполняемые
- Какие команды есть в системной папке

Запускай! 🚀










