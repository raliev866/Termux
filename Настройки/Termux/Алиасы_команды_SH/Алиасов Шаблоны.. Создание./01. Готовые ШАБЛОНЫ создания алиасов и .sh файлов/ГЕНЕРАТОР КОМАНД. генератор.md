## ✅ ДА! ПОЛНОСТЬЮ АВТОМАТИЗИРУЕМ!

Вот **финальная версия генератора**, который после создания скрипта **сам** регистрирует команду и проверяет её работу:

```bash
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# ГЕНЕРАТОР НОВЫХ КОМАНД ПО ШАБЛОНУ (АВТОМАТИЧЕСКИЙ)
# ==============================================
# Версия: 3.0 - ВСЁ ДЕЛАЕТ САМ!
# ==============================================

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m' # No Color

echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}   ГЕНЕРАТОР НОВЫХ КОМАНД (ПОЛНЫЙ АВТОМАТ)${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""

# -------------------- ВВОД ДАННЫХ --------------------
echo -e "${YELLOW}📋 ЗАПОЛНИТЕ ОБЩУЮ ИНФОРМАЦИЮ:${NC}"
echo ""

read -p "📅 Дата создания (Enter для 2026.02.22): " user_date
user_date=${user_date:-2026.02.22}

read -p "✍️ Автор (Enter для Рафаэль): " user_author
user_author=${user_author:-Рафаэль}

read -p "📝 Краткое описание: " user_description
user_description=${user_description:-"Конвертирует файлы"}

echo ""
echo -e "${YELLOW}⚙️ ПАРАМЕТРЫ КОНВЕРТАЦИИ:${NC}"
echo ""

read -p "📄 Исходный формат (md, txt, csv и т.д.): " input_format
input_format=${input_format:-md}

read -p "🎯 Целевой формат (html, docx, pdf, epub и т.д.): " output_format
output_format=${output_format:-html}

read -p "🔤 Название алиаса (команда): " alias_name
alias_name=${alias_name:-мдгтмл}

read -p "📁 Дополнительная папка в Documents: " user_folder
user_folder=${user_folder:-"03_md_${output_format}_${alias_name}"}

# Формируем пути (убираем возможные двойные слеши)
work_dir="/storage/emulated/0/Documents/${user_folder}"
work_dir=$(echo "$work_dir" | sed 's|//|/|g')

scripts_dir="/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH"
output_file="$scripts_dir/$alias_name.sh"

echo ""
echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}🔧 Генерация скрипта...${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""

# -------------------- СОЗДАНИЕ ФАЙЛА --------------------
cat > "$output_file" << EOF
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# $alias_name - Конвертер $input_format → $output_format
# ==============================================
# Дата создания: $user_date
# Автор: $user_author
# Описание: $user_description
# ==============================================

WORK_DIR="$work_dir"
INPUT_EXT="$input_format"
OUTPUT_EXT="$output_format"
FOLDER_SUFFIX=" - $output_format"

echo "=============================================="
echo "     $alias_name - Конвертер $input_format → $output_format"
echo "=============================================="
echo "📁 Рабочая папка: \$WORK_DIR"
echo ""

# Проверка pandoc (для конвертации)
if ! command -v pandoc &> /dev/null; then
    echo "📦 Устанавливаю pandoc..."
    pkg install -y pandoc
fi

# Создание папки
mkdir -p "\$WORK_DIR"

# Счётчики
FILES_FOUND=0
FILES_CONVERTED=0

echo "🔍 Поиск файлов .\$INPUT_EXT..."
echo ""

# Поиск и конвертация
find "\$WORK_DIR" -type f -name "*.\$INPUT_EXT" 2>/dev/null | while read full_path; do
    
    FILES_FOUND=\$((FILES_FOUND + 1))
    filename=\$(basename "\$full_path" .\$INPUT_EXT)
    filedir=\$(dirname "\$full_path")
    
    echo "📄 [\$FILES_FOUND] \$filename.\$INPUT_EXT"
    
    # Определение папки назначения
    if [ "\$filedir" = "\$WORK_DIR" ]; then
        output_dir="\$WORK_DIR"
    else
        rel_path="\${filedir#\$WORK_DIR/}"
        top_folder="\${rel_path%%/*}"
        rest_path="\${rel_path#\$top_folder}"
        target_folder="\$WORK_DIR/\${top_folder}\${FOLDER_SUFFIX}\${rest_path}"
        mkdir -p "\$target_folder"
        output_dir="\$target_folder"
    fi
    
    output_file="\$output_dir/\$filename.\$OUTPUT_EXT"
    
    pandoc "\$full_path" -o "\$output_file"
    
    if [ \$? -eq 0 ]; then
        chmod 644 "\$output_file"
        echo "      ✅ Готово: \$filename.\$OUTPUT_EXT"
        FILES_CONVERTED=\$((FILES_CONVERTED + 1))
    else
        echo "      ❌ Ошибка"
    fi
done

echo ""
echo "=============================================="
echo "✅ КОНВЕРТАЦИЯ ЗАВЕРШЕНА"
echo "📊 Найдено: \$FILES_FOUND, Сконвертировано: \$FILES_CONVERTED"
echo "=============================================="
EOF

# -------------------- ДАЕМ ПРАВА --------------------
chmod +x "$output_file"
echo -e "${GREEN}✅ Скрипт создан: $output_file${NC}"
echo ""

# -------------------- АВТОМАТИЧЕСКАЯ РЕГИСТРАЦИЯ --------------------
echo -e "${YELLOW}⚙️ АВТОМАТИЧЕСКАЯ РЕГИСТРАЦИЯ КОМАНДЫ...${NC}"
echo ""

# 1. Удаляем старые версии
unalias "$alias_name" 2>/dev/null
sed -i "/$alias_name/d" ~/.bashrc
rm -f ~/bin/"$alias_name" 2>/dev/null

# 2. Создаём папку bin если нет
mkdir -p ~/bin

# 3. Создаём симлинк (профессиональный способ)
ln -sf "$output_file" ~/bin/"$alias_name"
chmod +x ~/bin/"$alias_name"

# 4. Добавляем bin в PATH если ещё не добавлен
if ! echo "$PATH" | grep -q "$HOME/bin"; then
    echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
fi

# 5. Добавляем алиас (для надёжности)
echo "alias $alias_name='bash $output_file'" >> ~/.bashrc

# 6. Применяем изменения
source ~/.bashrc

echo -e "${GREEN}✅ Команда '$alias_name' зарегистрирована${NC}"
echo ""

# -------------------- ПРОВЕРКА --------------------
echo -e "${YELLOW}🔍 ПРОВЕРКА:${NC}"
echo ""

# Проверка через which
which_output=$(which "$alias_name" 2>/dev/null)
if [ -n "$which_output" ]; then
    echo -e "${GREEN}✅ which $alias_name → $which_output${NC}"
else
    echo -e "${RED}❌ which $alias_name → НЕ НАЙДЕНА${NC}"
fi

# Проверка через алиас
alias_output=$(alias | grep "$alias_name" 2>/dev/null)
if [ -n "$alias_output" ]; then
    echo -e "${GREEN}✅ alias $alias_name → $alias_output${NC}"
else
    echo -e "${RED}❌ alias $alias_name → НЕ НАЙДЕН${NC}"
fi

# Проверка через ls
if [ -f ~/bin/"$alias_name" ]; then
    echo -e "${GREEN}✅ Файл в ~/bin/ существует${NC}"
else
    echo -e "${RED}❌ Файл в ~/bin/ отсутствует${NC}"
fi

echo ""
echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}🏁 ВСЁ ГОТОВО! Команда '$alias_name' создана и зарегистрирована.${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""
echo -e "${YELLOW}📁 Рабочая папка:${NC} $work_dir"
echo -e "${YELLOW}🔤 Команда для запуска:${NC} $alias_name"
echo ""
echo -e "${GREEN}Пример:${NC}"
echo "   $alias_name"
echo ""
```

---

## 🚀 ЧТО ТЕПЕРЬ ДЕЛАЕТ ЭТОТ СКРИПТ:

| Этап | Действие |
|------|----------|
| 1 | Спрашивает данные по вашему шаблону |
| 2 | Создаёт `.sh` файл |
| 3 | **АВТОМАТИЧЕСКИ** удаляет старые версии |
| 4 | **АВТОМАТИЧЕСКИ** создаёт симлинк в `~/bin/` |
| 5 | **АВТОМАТИЧЕСКИ** добавляет алиас в `.bashrc` |
| 6 | **АВТОМАТИЧЕСКИ** настраивает PATH |
| 7 | **АВТОМАТИЧЕСКИ** проверяет, что всё работает |
| 8 | Выдаёт готовую команду |

---

## 🎯 ПОСЛЕ ЗАПУСКА:

```bash
мдепуб
```
сразу будет работать! Никаких ручных действий.



___






___




## 🔥 СРОЧНО! Генератор не найден, потому что вы его ещё не создали!

Выполните **ЭТУ КОМАНДУ** чтобы создать генератор:

```bash
cat > /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/генератор.sh << 'EOF'
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# ГЕНЕРАТОР НОВЫХ КОМАНД ПО ШАБЛОНУ (АВТОМАТИЧЕСКИЙ)
# ==============================================
# Версия: 3.0 - ВСЁ ДЕЛАЕТ САМ!
# ==============================================

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}   ГЕНЕРАТОР НОВЫХ КОМАНД (ПОЛНЫЙ АВТОМАТ)${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""

echo -e "${YELLOW}📋 ЗАПОЛНИТЕ ОБЩУЮ ИНФОРМАЦИЮ:${NC}"
echo ""

read -p "📅 Дата создания (Enter для 2026.02.22): " user_date
user_date=${user_date:-2026.02.22}

read -p "✍️ Автор (Enter для Рафаэль): " user_author
user_author=${user_author:-Рафаэль}

read -p "📝 Краткое описание: " user_description
user_description=${user_description:-"Конвертирует файлы"}

echo ""
echo -e "${YELLOW}⚙️ ПАРАМЕТРЫ КОНВЕРТАЦИИ:${NC}"
echo ""

read -p "📄 Исходный формат (md, txt, csv и т.д.): " input_format
input_format=${input_format:-md}

read -p "🎯 Целевой формат (html, docx, pdf, epub и т.д.): " output_format
output_format=${output_format:-html}

read -p "🔤 Название алиаса (команда): " alias_name
alias_name=${alias_name:-мдгтмл}

read -p "📁 Дополнительная папка в Documents: " user_folder
user_folder=${user_folder:-"03_md_${output_format}_${alias_name}"}

work_dir="/storage/emulated/0/Documents/${user_folder}"
work_dir=$(echo "$work_dir" | sed 's|//|/|g')

scripts_dir="/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH"
output_file="$scripts_dir/$alias_name.sh"

echo ""
echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}🔧 Генерация скрипта...${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""

cat > "$output_file" << SCRIPT
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# $alias_name - Конвертер $input_format → $output_format
# ==============================================
# Дата создания: $user_date
# Автор: $user_author
# Описание: $user_description
# ==============================================

WORK_DIR="$work_dir"
INPUT_EXT="$input_format"
OUTPUT_EXT="$output_format"
FOLDER_SUFFIX=" - $output_format"

echo "=============================================="
echo "     $alias_name - Конвертер $input_format → $output_format"
echo "=============================================="
echo "📁 Рабочая папка: \$WORK_DIR"
echo ""

if ! command -v pandoc &> /dev/null; then
    echo "📦 Устанавливаю pandoc..."
    pkg install -y pandoc
fi

mkdir -p "\$WORK_DIR"

FILES_FOUND=0
FILES_CONVERTED=0

echo "🔍 Поиск файлов .\$INPUT_EXT..."
echo ""

find "\$WORK_DIR" -type f -name "*.\$INPUT_EXT" 2>/dev/null | while read full_path; do
    
    FILES_FOUND=\$((FILES_FOUND + 1))
    filename=\$(basename "\$full_path" .\$INPUT_EXT)
    filedir=\$(dirname "\$full_path")
    
    echo "📄 [\$FILES_FOUND] \$filename.\$INPUT_EXT"
    
    if [ "\$filedir" = "\$WORK_DIR" ]; then
        output_dir="\$WORK_DIR"
    else
        rel_path="\${filedir#\$WORK_DIR/}"
        top_folder="\${rel_path%%/*}"
        rest_path="\${rel_path#\$top_folder}"
        target_folder="\$WORK_DIR/\${top_folder}\${FOLDER_SUFFIX}\${rest_path}"
        mkdir -p "\$target_folder"
        output_dir="\$target_folder"
    fi
    
    output_file="\$output_dir/\$filename.\$OUTPUT_EXT"
    
    pandoc "\$full_path" -o "\$output_file"
    
    if [ \$? -eq 0 ]; then
        chmod 644 "\$output_file"
        echo "      ✅ Готово: \$filename.\$OUTPUT_EXT"
        FILES_CONVERTED=\$((FILES_CONVERTED + 1))
    else
        echo "      ❌ Ошибка"
    fi
done

echo ""
echo "=============================================="
echo "✅ КОНВЕРТАЦИЯ ЗАВЕРШЕНА"
echo "📊 Найдено: \$FILES_FOUND, Сконвертировано: \$FILES_CONVERTED"
echo "=============================================="
SCRIPT

chmod +x "$output_file"
echo -e "${GREEN}✅ Скрипт создан: $output_file${NC}"
echo ""

echo -e "${YELLOW}⚙️ АВТОМАТИЧЕСКАЯ РЕГИСТРАЦИЯ КОМАНДЫ...${NC}"
echo ""

unalias "$alias_name" 2>/dev/null
sed -i "/$alias_name/d" ~/.bashrc
rm -f ~/bin/"$alias_name" 2>/dev/null

mkdir -p ~/bin
ln -sf "$output_file" ~/bin/"$alias_name"
chmod +x ~/bin/"$alias_name"

if ! echo "$PATH" | grep -q "$HOME/bin"; then
    echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
fi

echo "alias $alias_name='bash $output_file'" >> ~/.bashrc
source ~/.bashrc

echo -e "${GREEN}✅ Команда '$alias_name' зарегистрирована${NC}"
echo ""

echo -e "${YELLOW}🔍 ПРОВЕРКА:${NC}"
echo ""

which_output=$(which "$alias_name" 2>/dev/null)
if [ -n "$which_output" ]; then
    echo -e "${GREEN}✅ which $alias_name → $which_output${NC}"
else
    echo -e "${RED}❌ which $alias_name → НЕ НАЙДЕНА${NC}"
fi

alias_output=$(alias | grep "$alias_name" 2>/dev/null)
if [ -n "$alias_output" ]; then
    echo -e "${GREEN}✅ alias $alias_name → $alias_output${NC}"
else
    echo -e "${RED}❌ alias $alias_name → НЕ НАЙДЕН${NC}"
fi

if [ -f ~/bin/"$alias_name" ]; then
    echo -e "${GREEN}✅ Файл в ~/bin/ существует${NC}"
else
    echo -e "${RED}❌ Файл в ~/bin/ отсутствует${NC}"
fi

echo ""
echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}🏁 ВСЁ ГОТОВО! Команда '$alias_name' создана и зарегистрирована.${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""
echo -e "${YELLOW}📁 Рабочая папка:${NC} $work_dir"
echo -e "${YELLOW}🔤 Команда для запуска:${NC} $alias_name"
echo ""
echo -e "${GREEN}Пример:${NC}"
echo "   $alias_name"
echo ""
EOF
```

---

## ✅ ТЕПЕРЬ ВЫПОЛНИТЕ:

```bash
# Даем права на выполнение
chmod +x /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/генератор.sh

# Запускаем генератор
bash /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/генератор.sh
```

После этого генератор **сам** создаст команду и **сам** её зарегистрирует!