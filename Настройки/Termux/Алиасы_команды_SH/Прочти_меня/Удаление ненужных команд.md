







## ✅ **Как полностью удалить команду `скопировать_все` из Termux**

### 📋 **Полное удаление (все следы):**

```bash
# 1. Удалить симлинк из ~/bin
rm -f ~/bin/скопировать_все

# 2. Удалить сам скрипт из .termux_scripts
rm -f ~/.termux_scripts/скопировать_все.sh

# 3. Удалить алиас из .bashrc (если есть)
sed -i '/скопировать_все/d' ~/.bashrc

# 4. Удалить все возможные версии из домашней папки
rm -f ~/скопировать_все
rm -f ~/скопировать_все.sh

# 5. Применить изменения
source ~/.bashrc
```

### 🔍 **Проверка, что команда удалена:**

```bash
# Проверить, что симлинка нет
ls -la ~/bin/ | grep скопировать_все

# Проверить, что скрипта нет
ls -la ~/.termux_scripts/ | grep скопировать_все

# Проверить, что команда не доступна
which скопировать_все
# Должно показать: скопировать_все: command not found
```

### ⚡ **Если хотите удалить, но оставить исходный файл во внешнем хранилище:**

```bash
# Просто удалите из Termux, оставив оригинал в /storage/emulated/0/
rm -f ~/bin/скопировать_все
rm -f ~/.termux_scripts/скопировать_все.sh
sed -i '/скопировать_все/d' ~/.bashrc
source ~/.bashrc
```

Исходный файл останется здесь (если он там был):
```
/storage/emulated/0/Documents/01_Настройки_01_GitHub_DeepSeekProject/Алиасы_команды_SH/скопировать_все
```

### 🧹 **Полная очистка всех нерабочих команд (для порядка):**

```bash
# Посмотреть все команды в bin
ls -la ~/bin/

# Удалить конкретную
rm -f ~/bin/название_команды

# Удалить всё из ~/bin (осторожно!)
rm -f ~/bin/*

# Перерегистрировать только рабочие через ваш скрипт
# После этого можно заново запустить скопировать_все (если нужно)
```




