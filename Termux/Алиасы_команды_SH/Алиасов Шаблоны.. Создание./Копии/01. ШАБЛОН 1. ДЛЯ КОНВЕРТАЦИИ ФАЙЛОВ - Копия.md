
## 📋 **ШАБЛОН 1: ДЛЯ КОНВЕРТАЦИИ ФАЙЛОВ (заполняешь только эти поля)**

### **Общая информация**
* **Дата создания:** 2026.02.17 🔵 (текущая дата)
* **Автор:** Рафаэль 🔵 (твое имя/ник)
* **Краткое описание:** Конвертирует Markdown в  🔵 (например: "Конвертирует Markdown в Docx")

### **Параметры конвертации**
* **Исходный формат:** md 🔵 (md / txt / html / и т.д.)
* **Целевой формат:** 🔵 (docx / pdf / html / и т.д.)
* **Название алиаса:** 🔵 (например: md2docx)
* **Дополнительная папка в Documents:** 🔵 (например: "Книги" или оставить пустым)

---

### **Автоматически заполняется (моя часть):**


#### **0. Предварительный просмотр** 🔴
Перед созданием команды я показываю тебе полностью заполненный шаблон на основе твоих данных, чтобы ты мог проверить и утвердить.


#### **1.4 Рабочая папка**
* **Временная папка для сборки:** ~/temp_converter
* **Полный путь к рабочей папке:** /storage/emulated/0/Documents/ [твоя_папка] (если указана)
* **Папка для хранения скриптов:** /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/

#### **1.1 Идентификация команды**
* **Название файла .sh:** [название_алиаса].sh
* **Тип инструмента:** Конвертер
* **Формат входных данных:** файлы .[исходный формат]
* **Формат выходных данных:** файлы .[целевой формат]

#### **2.3 Правила для режима Б (файлы)**
* **Маска входных файлов:** *.[исходный формат]
* **Маска выходных файлов:** {name}.[целевой формат]
* **Исключения:** (пусто)


#### **2.5 Правила размещения выходных файлов**
* **Для файлов без папки:** Конвертированный файл сохраняется в той же папке
* **Для файлов в папках:** Создается **копия папки с суффиксом `" - "` Целевой формат**, куда сохраняются все сконвертированные файлы
* **Для вложенных папок:** Сохраняется **вся структура подпапок** внутри скопированной папки
* **Права доступа к файлам:** ✅ Файл НЕ должен быть "только для чтения" — все пользователи могут изменять файл (права 644: владелец может писать, остальные читать)
* **Пример:**
  ```
  Исходная структура:                  Структура после конвертации:
  Книга/                                Книга - html/
  ├── Глава1/                           ├── Глава1/
  │   ├── часть1.md                     │   ├── часть1.html
  │   └── часть2.md                     │   └── часть2.html
  ├── Глава2.md                         ├── Глава2.html
  └── Введение.md                        └── Введение.html


  ```

* **Исключения:** (можно указать папки, которые не нужно копировать

#### **3.1 Основные функции**
* **Функция 1:** Поиск всех файлов с расширением .[исходный формат] в рабочей папке
* **Функция 2:** Конвертация каждого файла в формат .[целевой формат]
* **Функция 3:** Сохранение результатов в той же папке

#### **4.1 Проверка зависимостей**
* **Необходимые пакеты:** (зависит от форматов, например: pandoc для docx, wkhtmltopdf для pdf)
* **Команда установки:** pkg install -y [пакеты]

#### **4.2 Создание скрипта**
* **Содержимое скрипта:** (генерируется под твои форматы с использованием pandoc/wkhtmltopdf/etc)

#### **4.3 Создание алиаса**
* **Команда:** echo "alias [название_алиаса]='bash /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/[название_алиаса].sh'" >> ~/.bashrc

#### **4.4 Создание симлинка**
* **Команда:** ln -sf /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/[название_алиаса].sh /data/data/com.termux/files/usr/bin/[название_алиаса]

#### **5.1 Тесты**
* **Тест 1:** Запустить конвертацию и проверить наличие выходных файлов
* **Тест 2:** Проверить содержимое сконвертированных файлов

#### **6.1 README**
* **Содержимое:** генерируется с указанием твоих форматов


## ✅ НОВЫЙ РАЗДЕЛ В ШАБЛОНЕ: ФИНАЛЬНАЯ НАСТРОЙКА И ЗАЩИТА

### **7.0 ФИНАЛЬНАЯ НАСТРОЙКА И ЗАЩИТА КОМАНД**

---

#### **7.1 Очистка от старых алиасов и конфликтов**

| Проблема | Решение | Команда |
|----------|---------|---------|
| **Старый алиас висит в памяти** | Удалить алиас из текущей сессии | `unalias мдгтмл 2>/dev/null` |
| **Старый алиас прописан в .bashrc** | Удалить все упоминания | `sed -i '/мдгтмл/d' ~/.bashrc` |
| **Старый симлинк в /bin** | Удалить физический файл | `rm -f ~/bin/мдгтмл 2>/dev/null` |
| **Старый скрипт в другой папке** | Найти и удалить | `find ~ -name "мдгтмл*" -type f 2>/dev/null | grep -v ".termux_scripts" | xargs rm -f` |
| **Кэш команд (hash)** | Очистить, чтобы система забыла старые пути | `hash -r` |

---

#### **7.2 Привязка только к вашей папке**

| Действие | Описание | Команда |
|----------|----------|---------|
| **Создать центральное хранилище** | Папка только для ваших скриптов | `mkdir -p ~/.termux_scripts` |
| **Скопировать все ваши .sh файлы** | Перенести из внешнего хранилища | `cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/` |
| **Дать права на выполнение** | Разрешить запуск | `chmod +x ~/.termux_scripts/*.sh` |
| **Заблокировать другие источники** | Убрать чужие пути из PATH | `export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"` |
| **Сохранить новый PATH навсегда** | Перезаписать .bashrc | `# Сохранить старый .bashrc в резервную копию
cp ~/.bashrc ~/.bashrc.backup

# Добавить новую настройку, не удаляя старую
echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' >> ~/.bashrc` |

---

#### **7.3 Автоматическая регистрация всех ваших команд**

| Действие | Описание | Команда |
|----------|----------|---------|
| **Создать папку bin** | Для симлинков | `mkdir -p ~/bin` |
| **Авто-симлинки на все скрипты** | Каждый .sh становится командой | `for script in ~/.termux_scripts/*.sh; do name=$(basename "$script" .sh); ln -sf "$script" ~/bin/"$name"; done` |
| **Права на симлинки** | Чтобы работали | `chmod +x ~/bin/*` |
| **Добавить bin в PATH** | Чтобы команды были видны | `echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc` |
| **Применить изменения** | Перезагрузить настройки | `source ~/.bashrc` |

---

#### **7.4 Диагностика и проверка**

| Проверка | Что должно быть | Команда |
|----------|-----------------|---------|
| **Есть ли конфликтующие алиасы?** | Пусто | `alias | grep -E "мдгтмл|мддок|мдпдф"` |
| **Где находится команда?** | Путь к ~/bin/ | `which мдгтмл` |
| **Правильный ли PATH?** | Только /usr/bin и ~/bin | `echo $PATH` |
| **Работает ли скрипт?** | Вывод без ошибок | `мдгтмл --version 2>/dev/null \|\| мдгтмл` |
| **Есть ли дубликаты?** | Один файл на команду | `ls -la ~/bin/ \| grep -E "мдгтмл|мддок|мдпдф"` |

---

#### **7.5 Аварийное восстановление (если всё сломалось)**

```bash
# Полный сброс и пересборка
unalias мдгтмл мддок мдпдф 2>/dev/null
sed -i '/мдгтмл\|мддок\|мдпдф/d' ~/.bashrc
rm -f ~/bin/мдгтмл ~/bin/мддок ~/bin/мдпдф
rm -rf ~/.termux_scripts 2>/dev/null

# Создать заново
mkdir -p ~/.termux_scripts
cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/
chmod +x ~/.termux_scripts/*.sh

for script in ~/.termux_scripts/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" ~/bin/"$name"
done

chmod +x ~/bin/*
echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' > ~/.bashrc
echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
source ~/.bashrc

echo "✅ Восстановление завершено"
```

---

#### **7.6 Защита от будущих проблем**

| Проблема | Защита |
|----------|--------|
| **Переустановка Termux** | Все скрипты хранятся в `/storage/emulated/0/` — они не пропадают |
| **Случайное удаление** | Сделать резервную копию папки `Алиасы_команды_SH` |
| **Конфликт с новыми пакетами** | PATH настроен так, что ваши команды в приоритете |
| **Забыл команду** | `ls ~/bin/` покажет все доступные команды |

---

#### **7.7 Полный финальный скрипт для восстановления**

Создайте файл `восстановить_все.sh` в папке с вашими скриптами:

```bash
#!/data/data/com.termux/files/usr/bin/bash
# Файл: восстановить_все.sh
# Описание: Полное восстановление всех команд после переустановки Termux

echo "🔄 НАЧАЛО ПОЛНОГО ВОССТАНОВЛЕНИЯ"
echo "==================================="

# 1. Очистка старых алиасов и команд
echo "[1/7] Очистка старых алиасов..."
unalias мдгтмл мддок мдпдф 2>/dev/null
sed -i '/мдгтмл\|мддок\|мдпдф/d' ~/.bashrc
rm -f ~/bin/мдгтмл ~/bin/мддок ~/bin/мдпдф

# 2. Создание папки для скриптов
echo "[2/7] Создание папки ~/.termux_scripts..."
mkdir -p ~/.termux_scripts

# 3. Копирование ваших скриптов
echo "[3/7] Копирование скриптов из внешнего хранилища..."
cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/ 2>/dev/null

# 4. Права на выполнение
echo "[4/7] Установка прав..."
chmod +x ~/.termux_scripts/*.sh

# 5. Создание симлинков
echo "[5/7] Создание симлинков в ~/bin..."
mkdir -p ~/bin
for script in ~/.termux_scripts/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" ~/bin/"$name"
    echo "   ✅ $name"
done
chmod +x ~/bin/*

# 6. Настройка PATH
echo "[6/7] Настройка PATH..."
echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' > ~/.bashrc
echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
source ~/.bashrc

# 7. Проверка
echo "[7/7] Проверка..."
echo "PATH: $PATH"
echo ""
echo "Доступные команды:"
ls -la ~/bin/ | grep -E "мдгтмл|мддок|мдпдф" || echo "❌ Команды не найдены!"

echo "==================================="
echo "✅ ВОССТАНОВЛЕНИЕ ЗАВЕРШЕНО"
echo "Проверьте команды:"
echo "   мдгтмл"
echo "   мддок"
echo "   мдпдф"
```

---

#### **7.8 Итоговый чек-лист после восстановления**

- [ ] `unalias мдгтмл` — не выдает ошибку
- [ ] `which мдгтмл` — показывает `~/bin/мдгтмл`
- [ ] `мдгтмл` — работает без ошибок
- [ ] `ls -la ~/bin/` — показывает только ваши команды
- [ ] `echo $PATH` — содержит `/data/data/com.termux/files/usr/bin` и `~/bin`
- [ ] В папке `~/.termux_scripts/` лежат копии всех ваших .sh файлов


## ✅ НОВЫЙ РАЗДЕЛ В ШАБЛОНЕ: ФИНАЛЬНАЯ НАСТРОЙКА И ЗАЩИТА

### **7.0 ФИНАЛЬНАЯ НАСТРОЙКА И ЗАЩИТА КОМАНД**

---

#### **7.1 Очистка от старых алиасов и конфликтов**

| Проблема | Решение | Команда |
|----------|---------|---------|
| **Старый алиас висит в памяти** | Удалить алиас из текущей сессии | `unalias мдгтмл 2>/dev/null` |
| **Старый алиас прописан в .bashrc** | Удалить все упоминания | `sed -i '/мдгтмл/d' ~/.bashrc` |
| **Старый симлинк в /bin** | Удалить физический файл | `rm -f ~/bin/мдгтмл 2>/dev/null` |
| **Старый скрипт в другой папке** | Найти и удалить | `find ~ -name "мдгтмл*" -type f 2>/dev/null | grep -v ".termux_scripts" | xargs rm -f` |
| **Кэш команд (hash)** | Очистить, чтобы система забыла старые пути | `hash -r` |

---

#### **7.2 Привязка только к вашей папке**

| Действие | Описание | Команда |
|----------|----------|---------|
| **Создать центральное хранилище** | Папка только для ваших скриптов | `mkdir -p ~/.termux_scripts` |
| **Скопировать все ваши .sh файлы** | Перенести из внешнего хранилища | `cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/` |
| **Дать права на выполнение** | Разрешить запуск | `chmod +x ~/.termux_scripts/*.sh` |
| **Заблокировать другие источники** | Убрать чужие пути из PATH | `export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"` |
| **Сохранить новый PATH навсегда** | Перезаписать .bashrc | `echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' > ~/.bashrc` |

---

#### **7.3 Автоматическая регистрация всех ваших команд**

| Действие | Описание | Команда |
|----------|----------|---------|
| **Создать папку bin** | Для симлинков | `mkdir -p ~/bin` |
| **Авто-симлинки на все скрипты** | Каждый .sh становится командой | `for script in ~/.termux_scripts/*.sh; do name=$(basename "$script" .sh); ln -sf "$script" ~/bin/"$name"; done` |
| **Права на симлинки** | Чтобы работали | `chmod +x ~/bin/*` |
| **Добавить bin в PATH** | Чтобы команды были видны | `echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc` |
| **Применить изменения** | Перезагрузить настройки | `source ~/.bashrc` |

---

#### **7.4 Диагностика и проверка**

| Проверка | Что должно быть | Команда |
|----------|-----------------|---------|
| **Есть ли конфликтующие алиасы?** | Пусто | `alias | grep -E "мдгтмл|мддок|мдпдф"` |
| **Где находится команда?** | Путь к ~/bin/ | `which мдгтмл` |
| **Правильный ли PATH?** | Только /usr/bin и ~/bin | `echo $PATH` |
| **Работает ли скрипт?** | Вывод без ошибок | `мдгтмл --version 2>/dev/null \|\| мдгтмл` |
| **Есть ли дубликаты?** | Один файл на команду | `ls -la ~/bin/ \| grep -E "мдгтмл|мддок|мдпдф"` |

---

#### **7.5 Аварийное восстановление (если всё сломалось)**

```bash
# Полный сброс и пересборка
unalias мдгтмл мддок мдпдф 2>/dev/null
sed -i '/мдгтмл\|мддок\|мдпдф/d' ~/.bashrc
rm -f ~/bin/мдгтмл ~/bin/мддок ~/bin/мдпдф
rm -rf ~/.termux_scripts 2>/dev/null

# Создать заново
mkdir -p ~/.termux_scripts
cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/
chmod +x ~/.termux_scripts/*.sh

for script in ~/.termux_scripts/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" ~/bin/"$name"
done

chmod +x ~/bin/*
echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' > ~/.bashrc
echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
source ~/.bashrc

echo "✅ Восстановление завершено"
```

---

#### **7.6 Защита от будущих проблем**

| Проблема | Защита |
|----------|--------|
| **Переустановка Termux** | Все скрипты хранятся в `/storage/emulated/0/` — они не пропадают |
| **Случайное удаление** | Сделать резервную копию папки `Алиасы_команды_SH` |
| **Конфликт с новыми пакетами** | PATH настроен так, что ваши команды в приоритете |
| **Забыл команду** | `ls ~/bin/` покажет все доступные команды |

---

#### **7.7 Полный финальный скрипт для восстановления**

Создайте файл `восстановить_все.sh` в папке с вашими скриптами:

```bash
#!/data/data/com.termux/files/usr/bin/bash
# Файл: восстановить_все.sh
# Описание: Полное восстановление всех команд после переустановки Termux

echo "🔄 НАЧАЛО ПОЛНОГО ВОССТАНОВЛЕНИЯ"
echo "==================================="

# 1. Очистка старых алиасов и команд
echo "[1/7] Очистка старых алиасов..."
unalias мдгтмл мддок мдпдф 2>/dev/null
sed -i '/мдгтмл\|мддок\|мдпдф/d' ~/.bashrc
rm -f ~/bin/мдгтмл ~/bin/мддок ~/bin/мдпдф

# 2. Создание папки для скриптов
echo "[2/7] Создание папки ~/.termux_scripts..."
mkdir -p ~/.termux_scripts

# 3. Копирование ваших скриптов
echo "[3/7] Копирование скриптов из внешнего хранилища..."
cp "/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/"*.sh ~/.termux_scripts/ 2>/dev/null

# 4. Права на выполнение
echo "[4/7] Установка прав..."
chmod +x ~/.termux_scripts/*.sh

# 5. Создание симлинков
echo "[5/7] Создание симлинков в ~/bin..."
mkdir -p ~/bin
for script in ~/.termux_scripts/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" ~/bin/"$name"
    echo "   ✅ $name"
done
chmod +x ~/bin/*

# 6. Настройка PATH
echo "[6/7] Настройка PATH..."
echo 'export PATH="/data/data/com.termux/files/usr/bin:$HOME/bin"' > ~/.bashrc
echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
source ~/.bashrc

# 7. Проверка
echo "[7/7] Проверка..."
echo "PATH: $PATH"
echo ""
echo "Доступные команды:"
ls -la ~/bin/ | grep -E "мдгтмл|мддок|мдпдф" || echo "❌ Команды не найдены!"

echo "==================================="
echo "✅ ВОССТАНОВЛЕНИЕ ЗАВЕРШЕНО"
echo "Проверьте команды:"
echo "   мдгтмл"
echo "   мддок"
echo "   мдпдф"
```

---

#### **7.8 Итоговый чек-лист после восстановления**

- [ ] `unalias мдгтмл` — не выдает ошибку
- [ ] `which мдгтмл` — показывает `~/bin/мдгтмл`
- [ ] `мдгтмл` — работает без ошибок
- [ ] `ls -la ~/bin/` — показывает только ваши команды
- [ ] `echo $PATH` — содержит `/data/data/com.termux/files/usr/bin` и `~/bin`
- [ ] В папке `~/.termux_scripts/` лежат копии всех ваших .sh файлов

---

#### **8.0 Известные проблемы и их решения**

| Проблема | Причина | Решение |
|----------|---------|---------|
| **Termux не видит команду после создания** | Старый алиас в памяти | `unalias имя_команды` |
| **Permission denied** | Нет прав на выполнение | `chmod +x ~/.termux_scripts/*.sh` |
| **Файлы не конвертируются** | Нет pandoc | `pkg install pandoc` |
| **Команда есть, но не та версия** | Конфликт с системной | `which команда` покажет путь |
| **После переустановки всё пропало** | Скрипты не скопированы | Запустить `восстановить_все.sh` |

#### **9.0 План действий при переустановке Termux**

1. Установить базовые пакеты: `pkg install coreutils termux-tools nano`
2. Дать доступ к хранилищу: `termux-setup-storage`
3. Запустить скрипт восстановления: `bash /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/восстановить_все.sh`
4. Проверить работу созданных команд.
5. # После восстановления выполнить:
Название команды --help 2>/dev/null || название команды


---











