


# Как установить в свой телефон эти шрифты?
# **Charter**,  **PT Serif**,  **Roboto**,  **Montserrat** 


Установка шрифтов в систему Android — задача со звездочкой, так как Google по соображениям безопасности ограничивает изменение системных шрифтов. Однако есть несколько способов сделать это, в зависимости от твоей цели.

### Способ 1: Для использования в документах (Word, Google Docs)

Если тебе нужно, чтобы эти шрифты появились в выпадающем списке приложений для работы с текстом:

1. **Microsoft Word / Office:**
* К сожалению, мобильный Word не умеет «подхватывать» шрифты из памяти телефона. Он использует только свои встроенные.
* **Решение:** Используй приложение **Collabora Office** или **WPS Office**. Они позволяют импортировать свои файлы шрифтов (`.ttf`) и использовать их в документах.


2. **Google Документы:**
* Шрифты **Roboto**, **Montserrat** и **PT Serif** уже встроены в Google Docs!
* Нажми на выбор шрифта -> **Другие шрифты** и в поиске набери их названия. Они сразу появятся. (Charter там обычно нет).



---

### Способ 2: Установка в систему (Чтобы всё меню было таким)

Это изменит шрифт во всем телефоне, но способ зависит от модели:

* **Samsung (zFont 3):** Скачай приложение **zFont 3** из Play Store. Оно позволяет упаковать любой `.ttf` файл (Charter, PT Serif) в формат Samsung и установить его через "Темы".
* **Xiaomi (MIUI):** Зайди в приложение «Темы» и в поиске шрифтов просто напиши **"Montserrat"** или **"PT Serif"**. Большинство этих шрифтов уже загружены в магазин тем Xiaomi умельцами.
* **Другие модели:** Если телефон не поддерживает смену тем, установить шрифт системно можно только с **Root-правами**, что я не рекомендую делать без опыта.

---

Шаг 3: Где взять сами файлы шрифтов?

Чтобы передать их приложениям, тебе нужны файлы формата `.ttf` или `.otf`.

1. **Roboto & Montserrat:** Скачай с [Google Fonts](https://fonts.google.com/).
2. **PT Serif:** Скачай с сайта [ParaType](https://www.paratype.ru/fonts/pt/pt-serif).
3. **Charter:** Ищи "Bitstream Charter TTF download".

---

### 💡 Лайфхак для твоего HTML-проекта

Поскольку ты активно работаешь в **Termux** и делаешь **HTML**, тебе **не нужно** устанавливать шрифты в телефон!

Мы можем сделать так, чтобы твой HTML-файл сам «подтягивал» эти шрифты из сети. Тогда, даже если в телефоне их нет, книга будет отображаться шрифтом **Charter** или **PT Serif**.

**Хочешь, я обновлю твою команду `мдвгтмл`, чтобы она сама прописывала эти шрифты в твои книги?** Тогда тебе вообще не придется ничего устанавливать в систему.

___


# Как установить в программы  эти шрифты (*. ttf)?
# **Charter**,  **PT Serif**,  **Roboto**,  **Montserrat** 

Установка шрифтов в систему Android — задача со звездочкой, так как Google по соображениям безопасности ограничивает изменение системных шрифтов. Однако есть несколько способов сделать это, в зависимости от твоей цели.

### Способ 1: Для использования в документах (Word, Google Docs)

Если тебе нужно, чтобы эти шрифты появились в выпадающем списке приложений для работы с текстом:

1. **Microsoft Word / Office:**
* К сожалению, мобильный Word не умеет «подхватывать» шрифты из памяти телефона. Он использует только свои встроенные.
* **Решение:** Используй приложение **Collabora Office** или **WPS Office**. Они позволяют импортировать свои файлы шрифтов (`.ttf`) и использовать их в документах.


2. **Google Документы:**
* Шрифты **Roboto**, **Montserrat** и **PT Serif** уже встроены в Google Docs!
* Нажми на выбор шрифта -> **Другие шрифты** и в поиске набери их названия. Они сразу появятся. (Charter там обычно нет).



---

### Способ 2: Установка в систему (Чтобы всё меню было таким)

Это изменит шрифт во всем телефоне, но способ зависит от модели:

* **Samsung (zFont 3):** Скачай приложение **zFont 3** из Play Store. Оно позволяет упаковать любой `.ttf` файл (Charter, PT Serif) в формат Samsung и установить его через "Темы".
* **Xiaomi (MIUI):** Зайди в приложение «Темы» и в поиске шрифтов просто напиши **"Montserrat"** или **"PT Serif"**. Большинство этих шрифтов уже загружены в магазин тем Xiaomi умельцами.
* **Другие модели:** Если телефон не поддерживает смену тем, установить шрифт системно можно только с **Root-правами**, что я не рекомендую делать без опыта.

---

### Шаг 3: Где взять сами файлы шрифтов?

Чтобы передать их приложениям, тебе нужны файлы формата `.ttf` или `.otf`.

1. **Roboto & Montserrat:** Скачай с [Google Fonts](https://fonts.google.com/).
2. **PT Serif:** Скачай с сайта [ParaType](https://www.paratype.ru/fonts/pt/pt-serif).
3. **Charter:** Ищи "Bitstream Charter TTF download".

---

### 💡 Лайфхак для твоего HTML-проекта

Поскольку ты активно работаешь в **Termux** и делаешь **HTML**, тебе **не нужно** устанавливать шрифты в телефон!

Мы можем сделать так, чтобы твой HTML-файл сам «подтягивал» эти шрифты из сети. Тогда, даже если в телефоне их нет, книга будет отображаться шрифтом **Charter** или **PT Serif**.

**Хочешь, я обновлю твою команду `мдвгтмл`, чтобы она сама прописывала эти шрифты в твои книги?** Тогда тебе вообще не придется ничего устанавливать в систему.

___

# Как встроить шрифты в приложения **Collabora Office** или **WPS Office**?

Для этих двух приложений подходы различаются: **Collabora** больше похожа на компьютерную программу и требует создания папки, а **WPS** ищет шрифты в памяти телефона.

Вот как это сделать:

### 1. Как установить шрифты в Collabora Office

Collabora (на базе LibreOffice) ищет шрифты в системных папках Android или в своей собственной директории внутри хранилища.

**Инструкция:**

1. Скачай файлы шрифтов в формате **.ttf** или **.otf** (например, `Charter.ttf`, `PTSerif.ttf`).
2. Открой любой файловый менеджер (например, встроенный или "CX Проводник").
3. Создай в корне внутренней памяти (/storage/emulated/0/) телефона папку с названием **`fonts`** (если её нет).
4. Скопируй туда свои шрифты.
5. **Полностью закрой** Collabora Office (выгрузи из списка запущенных приложений) и открой снова.
6. Теперь в выпадающем списке шрифтов должны появиться твои новые шрифты.

---

### 2. Как установить шрифты в WPS Office

WPS Office умеет сканировать память телефона на наличие шрифтов, но делает это по-своему.

**Инструкция:**

1. Положи файлы шрифтов в папку **`Documents/fonts`** или просто в корень папки **`Documents`**.
2. Открой WPS Office и создай/открой любой документ.
3. Нажми на инструмент выбора шрифта.
4. В самом низу списка или в меню настроек шрифтов найди кнопку **"Add Fonts"** (Добавить шрифты) или **"System fonts"**.
5. В некоторых версиях WPS нужно нажать на иконку «папки» в меню шрифтов, чтобы указать путь к файлу вручную.

---

### Где взять файлы для установки?

Чтобы ты не искал их по всему интернету, вот прямые наводки:

* **Roboto & Montserrat:** [fonts.google.com](https://fonts.google.com) (нажми "Download all").
* **PT Serif:** [paratype.ru](https://www.paratype.ru/fonts/pt/pt-serif).
* **Charter:** Ищи "Bitstream Charter TTF".

---

### 💡 Совет для твоей автоматизации

Поскольку ты работаешь в **Termux**, ты можешь скачать все эти шрифты одной командой прямо в нужную папку, чтобы Collabora их сразу увидела.

**Хочешь, я напишу короткую команду для Termux, которая сама скачает и разложит эти 4 шрифта по папкам, чтобы они заработали в офисных приложениях?**



___

# Как установить в телефон Xiaomi (MIUI) эти шрифты (*. ttf)?
# Charter,  PT Serif.



















___


# Как установить в свой файл-шаблон "--reference-doc" эти шрифты?
# **Charter**,  **PT Serif**,  **Roboto**,  **Montserrat** 

Чтобы установить эти шрифты в файл-шаблон для Pandoc, тебе нужно сначала установить их в саму систему Android (или Windows, если создаешь шаблон там), а затем «познакомить» с ними Word-файл.

Вот пошаговая инструкция, как подготовить такой эталонный документ.

---

### Шаг 1: Подготовка (Где взять шрифты)

Pandoc не сможет вставить шрифт в документ, если этого шрифта нет в системе, где запускается конвертация.

* **Roboto** и **Montserrat** — это Google Fonts.
* **PT Serif** — шрифт от ParaType.
* **Charter** — классический шрифт (ищи версию *Bitstream Charter* или *ITC Charter*).

### Шаг 2: Создание файла-шаблона (Reference Doc)

1. Открой Microsoft Word (или LibreOffice) и создай **пустой документ**. Назови его, например, `custom-reference.docx`.
2. Напечатай несколько строк текста: один заголовок и один обычный абзац.
3. **Главный секрет:** Тебе нужно менять не просто оформление текста, а **настройки Стилей**.
* Выдели заголовок -> Найди в меню «Стили» -> Правой кнопкой на «Заголовок 1» (Heading 1) -> **Изменить**.
* Там выбери шрифт (например, **Montserrat** для заголовков).
* Выдели обычный текст -> Стиль «Обычный» (Normal) -> **Изменить**.
* Выбери шрифт для чтения (например, **Charter** или **PT Serif**), цвет — **ярко-чёрный**, выравнивание — **по ширине**.



### Шаг 3: Как прописать это в твой скрипт

Теперь, когда у тебя есть файл `custom-reference.docx` с настроенными шрифтами, положи его в папку с проектом. Тебе нужно изменить команду в скрипте, добавив путь к этому шаблону.

В твоём скрипте (например, в команде `книга`) строка конвертации должна выглядеть так:

```bash
# Пример команды с использованием шаблона
pandoc "$file" --reference-doc="custom-reference.docx" -o "$OUT_DIR/$name.docx"

```

---

### Важный нюанс для Android (Termux)

Если ты конвертируешь файлы прямо в Termux, Pandoc при создании `.docx` **не встраивает** сами файлы шрифтов внутрь документа. Он просто записывает: *«Этот текст должен быть шрифтом Charter»*.

* Если ты откроешь этот файл на телефоне, где **Charter** не установлен, Android заменит его на стандартный (Roboto).
* Чтобы увидеть именно эти шрифты, они должны быть установлены в системе того устройства, на котором ты **открываешь** файл.

---

### Как это сделать в HTML (Скрипт `мдвгтмл`)

В HTML всё проще — мы можем подключить эти шрифты из интернета, и они будут отображаться у всех. Давай обновим твой блок стилей в скрипте `мдвгтмл`:

```bash
# Добавь это в свой скрипт мдвгтмл вместо старого STYLE_FILE
STYLE_FILE=$(mktemp)
cat << 'EOF' > "$STYLE_FILE"
@import url('https://fonts.googleapis.com/css2?family=Montserrat:wght@700&family=PT+Serif&family=Roboto&display=swap');

body { 
    font-family: 'PT Serif', serif; /* Шрифт для чтения */
    color: #000000 !important; 
    line-height: 1.6;
    max-width: 800px;
    margin: 0 auto;
    padding: 20px;
}

h1, h2, h3 { 
    font-family: 'Montserrat', sans-serif; /* Шрифт для заголовков */
    color: #000000;
}
EOF

```

**С каким форматом сейчас приоритетнее разобраться — чтобы в Word всё сразу было красиво или чтобы HTML выглядел как дорогая книга?**


___

















