


# Если скрипт программа находится в одном файле, тогда каким образом повреждаются эти команды и скрипты?


Отличный и очень важный вопрос! Давай разберемся, почему при таком количестве скриптов (которые все лежат в одном месте — в папке `~/`) команды могут переставать работать. Это поможет тебе в будущем быстро находить и исправлять проблемы.

Представь, что каждый твой скрипт (например, `~/док`, `~/все`) — это отдельный инструмент в твоем ящике. Чтобы они работали, должны быть соблюдены три простых условия.

### 🔍 Почему команда может не работать: 3 главные причины

#### 1. Файл скрипта «не виден» системе (Проблемы с `PATH` и алиасами)
Когда ты вводишь в Termux команду `док`, система ищет программу с таким именем в специальных папках, список которых хранится в переменной `PATH` . Твоя домашняя папка (`~/`) туда по умолчанию не входит. Поэтому мы и используем **алиасы** — они как ярлыки, которые говорят системе: «Когда пользователь пишет "док", на самом деле запусти файл `~/док`» .

**Как ломается:**
*   **Не подгрузился `.bashrc`**. Все алиасы хранятся в файле `~/.bashrc` . Если ты отредактировал этот файл, но не выполнил команду `source ~/.bashrc` или не перезапустил Termux, новые алиасы не активируются, и команда `индекс` или `word` будет недоступна, даже если сам файл скрипта лежит на месте .
*   **Алиас «сломался»**. Если ты переименовал или переместил скрипт `~/create_double_indexes.sh`, но алиас `индекс` в `.bashrc` продолжает указывать на старый адрес — команда работать не будет.

#### 2. У скрипта нет прав на исполнение (Проблема `Permission denied`)
Даже если система знает, где лежит скрипт, она не сможет его запустить, если у файла нет специального разрешения — права на исполнение (`+x`) .

**Как ломается:**
*   Если ты создал новый скрипт через `cat > ...`, но забыл дать команду `chmod +x имя_скрипта`, то при попытке его запустить ты получишь ошибку `Permission denied` . Твой скрипт `~/проверка` как раз показал, что у всех твоих файлов такие права есть — это отлично!
*   Иногда права могут «слететь» при копировании файлов с компьютера или флешки.

#### 3. Скрипт пытается работать с неправильным путём (Проблема `No such file or directory`)
Самая частая причина, по которой твой скрипт `док` долго не работал. Скрипт внутри себя содержит команды перехода (`cd`) или обращения к файлам по конкретным адресам.

**Как ломается:**
*   Если внутри скрипта жестко прописан путь `cd "/storage/emulated/0/Documents/08. Из .md в .docx. док"`, а ты переименовал папку в `08_Из_md_в_docx_док`, скрипт просто не найдет, куда идти, и выдаст ошибку.
*   То же самое с командой `проект` — она пыталась перейти в старую, уже несуществующую папку.

### 📝 Резюме для тебя

Когда какая-то команда перестает работать, ты теперь знаешь, где искать проблему. Вот твой чек-лист:

1.  **Проверь алиас:** Работает ли команда `индекс`? Если нет, открой файл `~/.bashrc` и посмотри, есть ли там строчка `alias индекс='...'`. После изменений всегда делай `source ~/.bashrc` .
2.  **Проверь права:** Есть ли у файла скрипта права на запуск? Твой скрипт `~/проверка` подсвечивает это зелеными галочками.
3.  **Проверь пути:** Самый важный пункт. Открой скрипт (`cat ~/имя_скрипта`) и посмотри на все строчки с `cd` или прямыми путями к файлам. Существует ли эта папка? Правильно ли она называется?

Ты уже проделал огромную работу по созданию и отладке этих скриптов, и теперь у тебя есть целый арсенал инструментов и понимание того, как они устроены. Это главное!











# Как сделать так чтобы скрипты, команды не повреждались?




Отличный вопрос! Давай разберем **как защитить скрипты от повреждений** и сделать так, чтобы они работали всегда.

## 🛡️ **5 СПОСОБОВ ЗАЩИТЫ СКРИПТОВ**

### 1. **Сделай скрипты только для чтения** (защита от случайного изменения)

```bash
# Защитить один скрипт
chmod 555 ~/док
chmod 555 ~/все
chmod 555 ~/оглавления

# Защитить все скрипты сразу
chmod 555 ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления 2>/dev/null
```

**Что это даёт:** Скрипты можно будет только **запускать**, но нельзя случайно изменить или удалить.

### 2. **Создай резервную копию всех скриптов** (быстрое восстановление)

```bash
# Создать папку для бэкапов
mkdir -p ~/backup_scripts

# Скопировать туда все скрипты
cp ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления ~/backup_scripts/ 2>/dev/null

# Посмотреть, что сохранилось
ls -la ~/backup_scripts/
```

### 3. **Создай скрипт для автоматического восстановления**

```bash
cat > ~/восстановить << 'EOF'
#!/bin/bash

GREEN='\033[0;32m'
RED='\033[0;31m'
NC='\033[0m'

echo "🔄 Восстанавливаю все скрипты из бэкапа..."

if [ -d ~/backup_scripts ]; then
    cp ~/backup_scripts/* ~/ 2>/dev/null
    chmod +x ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления 2>/dev/null
    echo -e "${GREEN}✅ Восстановление завершено!${NC}"
else
    echo -e "${RED}❌ Папка с бэкапами не найдена!${NC}"
fi
EOF

chmod +x ~/восстановить
```

### 4. **Сохрани список всех алиасов** (чтобы не потерять)

```bash
# Сохранить все алиасы в файл
alias > ~/my_aliases.txt

# Посмотреть сохранённые алиасы
cat ~/my_aliases.txt
```

### 5. **Создай "золотой" скрипт, который пересоздаст всё заново**

```bash
cat > ~/золотой << 'EOF'
#!/bin/bash

# ==============================================
# ЗОЛОТОЙ СКРИПТ - ВОССТАНАВЛИВАЕТ ВСЁ И СРАЗУ
# ==============================================

echo "🔨 Восстанавливаю все скрипты..."

# 1. Основные скрипты
cat > ~/док << 'INNER'
#!/bin/bash
cd "/storage/emulated/0/Documents/08_Из_md_в_docx_док" 2>/dev/null || { echo "❌ Папка не найдена"; exit 1; }
find . -name "*.md" -type f | while read file; do
    if [[ "$file" == *"- док"* ]]; then continue; fi
    dir=$(dirname "$file")
    parent=$(dirname "$dir")
    base=$(basename "$dir")
    target_dir="$parent/$base - док"
    mkdir -p "$target_dir"
    name=$(basename "$file" .md)
    pandoc "$file" -o "$target_dir/$name.docx"
done
echo "✅ Готово!"
INNER

cat > ~/все << 'INNER'
#!/bin/bash
BASE_DIR="/storage/emulated/0/Documents/01_GitHub_DeepSeekProject"
cd "$BASE_DIR" || exit 1
find . -type f | while read file; do
    echo "$file"
done
echo "✅ Полный обзор завершён"
INNER

# 2. Алиасы
cat >> ~/.bashrc << 'ALIASES'
alias индекс='~/create_double_indexes.sh'
alias оглавления='~/все'
alias док='~/док'
alias проект='cd /storage/emulated/0/Documents/01_GitHub_DeepSeekProject'
alias word='for f in *.md; do if [ -f "$f" ]; then pandoc "$f" -o "${f%.md}.docx"; fi; done'
alias мд='~/md2docx.sh'
alias формат='~/format.sh'
ALIASES

# 3. Права на исполнение
chmod +x ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления 2>/dev/null

# 4. Перезагрузка алиасов
source ~/.bashrc

echo "✅ ВСЁ ВОССТАНОВЛЕНО!"
echo "🔄 Перезапусти Termux или выполни: source ~/.bashrc"
EOF

chmod +x ~/золотой
```

## 📋 **ЧТО ТЕПЕРЬ У ТЕБЯ ЕСТЬ:**

| Команда | Что делает |
|---------|------------|
| **`~/золотой`** | Восстанавливает **все** скрипты заново |
| **`~/восстановить`** | Восстанавливает из резервной копии |
| **`~/backup_scripts/`** | Папка с копиями всех скриптов |
| **`~/my_aliases.txt`** | Файл со списком всех алиасов |

## ✅ **КАК ПОЛЬЗОВАТЬСЯ:**

### Раз в неделю делай бэкап:
```bash
cp ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления ~/backup_scripts/ 2>/dev/null
```

### Если что-то сломалось:
```bash
# Вариант 1: восстановить из бэкапа
~/восстановить

# Вариант 2: восстановить всё заново
~/золотой
```

### Защитить скрипты от изменений:
```bash
chmod 555 ~/*.sh ~/док ~/все ~/обзор ~/оглавление ~/оглавления 2>/dev/null
```

## 🎯 **ГЛАВНОЕ:**
Теперь у тебя есть **полная защита**:
- ✅ Резервные копии
- ✅ Скрипт восстановления
- ✅ "Золотой" скрипт на все случаи жизни
- ✅ Защита от случайных изменений

**Никакие команды больше не потеряются!** 🔥


