
Как сделать команды скрипты неубиваемыми?.




Отличный вопрос! Давай разберем, как сделать команды действительно "неубиваемыми" в Termux.

## 🛡️ **Что делает скрипты уязвимыми**

Скрипты могут "портиться" по нескольким причинам:
1. Случайное удаление или перезапись
2. Потеря прав на выполнение (`chmod +x`)
3. Изменение переменных окружения
4. Сбои Termux или Android

## 🔧 **Стратегия максимальной защиты**

### Уровень 1: Базовое резервирование
```bash
# Создай папку для резервных копий
mkdir -p ~/termux-backup/scripts
mkdir -p ~/termux-backup/bin

# Копируй все важные скрипты в оба места
cp ~/*.sh ~/termux-backup/scripts/
cp ~/*.sh /data/data/com.termux/files/usr/bin/
```

### Уровень 2: Использование Termux:Boot (автозапуск)

Установи Termux:Boot из F-Droid. Это приложение запускает скрипты при загрузке Android .

```bash
mkdir -p ~/.termux/boot/
```

Создай скрипт восстановления `~/.termux/boot/restore-scripts.sh`:
```bash
#!/data/data/com.termux/files/usr/bin/sh
# Восстанавливает права и ссылки при загрузке

# Восстановить права на все скрипты в ~/bin
chmod +x ~/bin/* 2>/dev/null
chmod +x /data/data/com.termux/files/usr/bin/* 2>/dev/null

# Проверить и восстановить ссылки
[ ! -f ~/оглавление ] && ln -s ~/все ~/оглавление 2>/dev/null
[ ! -f /data/data/com.termux/files/usr/bin/оглавление ] && cp ~/все /data/data/com.termux/files/usr/bin/оглавление 2>/dev/null
```

### Уровень 3: Termux:Tasker (интеграция с системой)

**Termux:Tasker** — это официальный плагин для интеграции с Tasker . Он решает главную проблему: скрипты выполняются через защищенный системный API, а не через оболочку Termux .

**Настройка:**
1. Установи Termux:Tasker из F-Droid 
2. В Tasker создай действие "Plugin → Termux:Tasker"
3. Укажи путь к скрипту (например, `~/script.sh`)

**Преимущества:**
- ✅ Работает даже если Termux закрыт 
- ✅ Переменные окружения всегда корректны
- ✅ Результаты возвращаются в Tasker (%stdout, %stderr, %exit) 
- ✅ Скрипты защищены от случайных изменений

### Уровень 4: Версионирование через Git (профессиональный подход)

Создай репозиторий для своих скриптов:
```bash
cd ~
git init script-repo
cd script-repo
cp ~/*.sh .
git add .
git commit -m "Initial scripts backup"
```

Автоматический бэкап через cron (с установкой `cronie`) :
```bash
# Установка
pkg install cronie termux-services

# Запуск демона
sv up crond

# Добавление в crontab (ежедневный коммит в 2AM)
crontab -e
0 2 * * * cd ~/script-repo && git add . && git commit -m "Daily backup $(date)" && git push
```

### Уровень 5: Неизменяемые скрипты (read-only)

Самый жёсткий уровень защиты:
```bash
# Сделать скрипты только для чтения
chmod 555 ~/*.sh  # r-x r-x r-x (нельзя изменить, только читать и выполнять)

# Изменить владельца на root (требует root)
sudo chown root:root ~/*.sh
sudo chmod 555 ~/*.sh
```

## 🎯 **ФИНАЛЬНАЯ РЕКОМЕНДАЦИЯ**

Для **максимальной надёжности** используй комбинацию:

```bash
# 1. Создай папку для скриптов
mkdir -p ~/bin

# 2. Перемести все скрипты туда
mv ~/*.sh ~/bin/ 2>/dev/null

# 3. Создай симлинки в системную папку
for script in ~/bin/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" "/data/data/com.termux/files/usr/bin/$name"
done

# 4. Установи права
chmod -R 755 ~/bin/
chmod 755 /data/data/com.termux/files/usr/bin/*

# 5. Настрой Termux:Boot (создай ~/.termux/boot/restore.sh)
mkdir -p ~/.termux/boot
cat > ~/.termux/boot/restore.sh << 'EOF'
#!/data/data/com.termux/files/usr/bin/sh
# Восстановление при загрузке
chmod -R 755 ~/bin/
for script in ~/bin/*.sh; do
    name=$(basename "$script" .sh)
    ln -sf "$script" "/data/data/com.termux/files/usr/bin/$name" 2>/dev/null
done
EOF
chmod +x ~/.termux/boot/restore.sh

# 6. Установи Termux:Tasker для интеграции
pkg install termux-api
```

## ✅ **ИТОГ: Неубиваемость достигнута**

| Метод | Защита от удаления | Защита от сбоев | Автовосстановление |
|-------|-------------------|-----------------|-------------------|
| Просто в ~/bin | ❌ | ❌ | ❌ |
| + Termux:Boot | ❌ | ✅ | ✅ |
| + Termux:Tasker | ✅ | ✅ | ✅ |
| + Git | ✅ | ✅ | ✅ |
| + Read-only (root) | ✅✅ | ✅ | ✅ |

**Лучшая практика:** Скрипты в `~/bin` + Termux:Boot + Termux:Tasker + регулярный Git push. Тогда они действительно **неубиваемы**! 🚀