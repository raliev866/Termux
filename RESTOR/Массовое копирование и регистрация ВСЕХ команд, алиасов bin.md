


## ✅ **ДА! Вот универсальный скрипт для массового копирования и регистрации ВСЕХ команд**

Этот скрипт найдёт **все файлы без расширения** в указанных папках и автоматически зарегистрирует их в Termux.

## 📋 **СКРИПТ: массовый копировщик и регистратор**

```bash
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# МАССОВЫЙ КОПИРОВЩИК И РЕГИСТРАТОР КОМАНД
# Версия: 1.0
# ==============================================

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${BLUE}══════════════════════════════════════════════${NC}"
echo -e "${GREEN}   МАССОВОЕ КОПИРОВАНИЕ И РЕГИСТРАЦИЯ КОМАНД${NC}"
echo -e "${BLUE}══════════════════════════════════════════════${NC}\n"

# ===== ПУТИ =====
BASE_DIR="/storage/emulated/0/Documents/01_Настройки_01_GitHub_DeepSeekProject/Алиасы_команды_SH"
BIN_DIR="$BASE_DIR/bin"
TERMUX_SCRIPTS="$HOME/.termux_scripts"
TERMUX_BIN="$HOME/bin"

# Создаём необходимые папки в Termux
mkdir -p "$TERMUX_SCRIPTS" "$TERMUX_BIN"
echo -e "${GREEN}✅ Папки созданы:${NC}"
echo "   $TERMUX_SCRIPTS"
echo "   $TERMUX_BIN\n"

# ===== ПРОВЕРКА PATH =====
if ! echo "$PATH" | grep -q "$HOME/bin"; then
    echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
    export PATH="$PATH:$HOME/bin"
    echo -e "${YELLOW}📝 PATH настроен (добавлен ~/bin)${NC}\n"
fi

# ===== СЧЁТЧИКИ =====
TOTAL_FILES=0
COPIED_FILES=0
REGISTERED_FILES=0
SKIPPED_FILES=0

# ===== ФУНКЦИЯ РЕГИСТРАЦИИ =====
register_command() {
    local source_file="$1"
    local filename=$(basename "$source_file")
    
    # Проверяем, что файл существует и не имеет расширения .sh
    if [[ "$filename" == *".sh" ]]; then
        echo -e "   ${YELLOW}⏭️ Пропускаю .sh файл: $filename${NC}"
        SKIPPED_FILES=$((SKIPPED_FILES + 1))
        return 1
    fi
    
    # Проверяем, что это исполняемый файл (если нет - делаем)
    if [ ! -x "$source_file" ]; then
        chmod +x "$source_file"
    fi
    
    # Целевой путь в Termux
    target_file="$TERMUX_SCRIPTS/$filename"
    
    # Копируем файл
    cp "$source_file" "$target_file" 2>/dev/null
    if [ $? -eq 0 ]; then
        COPIED_FILES=$((COPIED_FILES + 1))
        echo -e "   ${GREEN}✅ Скопировано:${NC} $filename"
    else
        echo -e "   ${RED}❌ Ошибка копирования:${NC} $filename"
        return 1
    fi
    
    # Даём права
    chmod +x "$target_file"
    
    # Удаляем старую команду если была
    unalias "$filename" 2>/dev/null
    sed -i "/$filename/d" ~/.bashrc
    rm -f "$TERMUX_BIN/$filename" 2>/dev/null
    
    # Создаём симлинк
    ln -sf "$target_file" "$TERMUX_BIN/$filename"
    
    # Добавляем алиас (на всякий случай)
    echo "alias $filename='$TERMUX_BIN/$filename'" >> ~/.bashrc
    
    REGISTERED_FILES=$((REGISTERED_FILES + 1))
    
    return 0
}

# ===== ПОИСК ФАЙЛОВ В MAIN ПАПКЕ =====
echo -e "${YELLOW}📁 Поиск в основной папке:${NC} $BASE_DIR"
echo ""

if [ -d "$BASE_DIR" ]; then
    for file in "$BASE_DIR"/*; do
        if [ -f "$file" ] && [ ! -x "$file" ] && [[ ! "$file" == *".sh" ]]; then
            TOTAL_FILES=$((TOTAL_FILES + 1))
            filename=$(basename "$file")
            echo -e "${BLUE}[$TOTAL_FILES]${NC} Найден: $filename"
            register_command "$file"
            echo ""
        fi
    done
else
    echo -e "${RED}❌ Папка не найдена: $BASE_DIR${NC}\n"
fi

# ===== ПОИСК ФАЙЛОВ В BIN ПАПКЕ =====
echo -e "${YELLOW}📁 Поиск в папке bin:${NC} $BIN_DIR"
echo ""

if [ -d "$BIN_DIR" ]; then
    for file in "$BIN_DIR"/*; do
        if [ -f "$file" ] && [ ! -x "$file" ] && [[ ! "$file" == *".sh" ]]; then
            TOTAL_FILES=$((TOTAL_FILES + 1))
            filename=$(basename "$file")
            echo -e "${BLUE}[$TOTAL_FILES]${NC} Найден: $filename"
            register_command "$file"
            echo ""
        fi
    done
else
    echo -e "${RED}❌ Папка не найдена: $BIN_DIR${NC}\n"
fi

# ===== ИТОГИ =====
echo -e "${BLUE}══════════════════════════════════════════════${NC}"
echo -e "${GREEN}📊 СТАТИСТИКА${NC}"
echo -e "${BLUE}══════════════════════════════════════════════${NC}"
echo -e "📁 Всего найдено файлов: ${YELLOW}$TOTAL_FILES${NC}"
echo -e "📋 Скопировано в Termux: ${GREEN}$COPIED_FILES${NC}"
echo -e "🔧 Зарегистрировано команд: ${GREEN}$REGISTERED_FILES${NC}"
echo -e "⏭️ Пропущено (.sh файлы): ${YELLOW}$SKIPPED_FILES${NC}"
echo ""

# ===== СПИСОК ЗАРЕГИСТРИРОВАННЫХ КОМАНД =====
if [ $REGISTERED_FILES -gt 0 ]; then
    echo -e "${YELLOW}🔍 Доступные команды:${NC}"
    for cmd in "$TERMUX_BIN"/*; do
        if [ -L "$cmd" ]; then
            echo "   • $(basename "$cmd")"
        fi
    done
    echo ""
fi

# ===== ПРОВЕРКА =====
echo -e "${YELLOW}🔍 Проверка нескольких команд:${NC}"
for cmd in генератор mdhtml mdfb2 мддок; do
    if command -v "$cmd" &>/dev/null; then
        echo -e "   ${GREEN}✅ $cmd найден: $(which "$cmd")${NC}"
    else
        echo -e "   ${RED}❌ $cmd не найден${NC}"
    fi
done

echo ""
echo -e "${BLUE}══════════════════════════════════════════════${NC}"
echo -e "${GREEN}🏁 ГОТОВО! Все команды скопированы и зарегистрированы${NC}"
echo -e "${BLUE}══════════════════════════════════════════════${NC}"
echo ""
echo -e "${YELLOW}💡 Теперь можно использовать команды:${NC}"
echo "   генератор  - создать новую команду"
echo "   mdhtml     - конвертировать md в html"
echo "   mdfb2      - конвертировать md в fb2"
echo "   и другие..."
```

---

## 🚀 **КАК ИСПОЛЬЗОВАТЬ:**

### **Способ 1: Сохранить и запустить**

```bash
# 1. Сохраните скрипт в удобное место (например, во внешнем хранилище)
nano /storage/emulated/0/Documents/01_Настройки_01_GitHub_DeepSeekProject/Алиасы_команды_SH/скопировать_все.sh

# 2. Вставьте код выше, сохраните (Ctrl+O, Enter) и выйдите (Ctrl+X)

# 3. Запустите из Termux:
bash /storage/emulated/0/Documents/01_Настройки_01_GitHub_DeepSeekProject/Алиасы_команды_SH/скопировать_все.sh
```

### **Способ 2: Создать прямо в Termux**

```bash
# 1. Создайте файл прямо в Termux
nano ~/скопировать_все.sh

# 2. Вставьте код выше, сохраните (Ctrl+O, Enter) и выйдите (Ctrl+X)

# 3. Сделайте исполняемым и запустите
chmod +x ~/скопировать_все.sh
./скопировать_все.sh
```

### **Способ 3: Сделать его командой**

```bash
# После создания, зарегистрируйте как команду
cp ~/скопировать_все.sh ~/.termux_scripts/
chmod +x ~/.termux_scripts/скопировать_все.sh
ln -sf ~/.termux_scripts/скопировать_все.sh ~/bin/скопировать_все

# Теперь можно запускать просто:
скопировать_все
```

---

## ✅ **ЧТО ДЕЛАЕТ ЭТОТ СКРИПТ:**

1. **Находит все файлы без расширения** в обеих папках
2. **Копирует их** в `~/.termux_scripts/`
3. **Даёт права** на выполнение
4. **Создаёт симлинки** в `~/bin/`
5. **Добавляет алиасы** в `.bashrc`
6. **Настраивает PATH**, если нужно
7. **Показывает статистику** и проверяет результат

**Одной командой — все файлы зарегистрированы!**



