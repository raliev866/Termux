
# Если алиас, команда не работает, то скопируй их в папку Termux. От туда они сразу запускаются.

___

## ✅ Скрипт массового копирования всех .sh файлов

```bash
#!/data/data/com.termux/files/usr/bin/bash

# ==============================================
# МАССОВОЕ КОПИРОВАНИЕ ВСЕХ .sh ФАЙЛОВ
# ==============================================
# Этот скрипт скопирует ВСЕ ваши .sh файлы
# из внешнего хранилища в Termux и создаст команды
# ==============================================

# Цвета для вывода
GREEN='\033[0;32m'
BLUE='\033[0;34m'
YELLOW='\033[1;33m'
RED='\033[0;31m'
NC='\033[0m'

echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}   МАССОВОЕ КОПИРОВАНИЕ .sh ФАЙЛОВ${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""

# -------------------- ИСХОДНАЯ ПАПКА --------------------
SOURCE_DIR="/storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH"

# -------------------- ЦЕЛЕВЫЕ ПАПКИ --------------------
TARGET_DIR="$HOME/.termux_scripts"
BIN_DIR="$HOME/bin"

# -------------------- ПРОВЕРКА ИСХОДНОЙ ПАПКИ --------------------
if [ ! -d "$SOURCE_DIR" ]; then
    echo -e "${RED}❌ Папка не найдена:${NC}"
    echo "   $SOURCE_DIR"
    exit 1
fi

echo -e "${YELLOW}📁 Исходная папка:${NC} $SOURCE_DIR"
echo -e "${YELLOW}📁 Целевая папка:${NC} $TARGET_DIR"
echo -e "${YELLOW}📁 Папка bin:${NC} $BIN_DIR"
echo ""

# -------------------- СОЗДАНИЕ ЦЕЛЕВЫХ ПАПОК --------------------
mkdir -p "$TARGET_DIR"
mkdir -p "$BIN_DIR"
echo -e "${GREEN}✅ Папки созданы${NC}"
echo ""

# -------------------- ПОИСК .sh ФАЙЛОВ --------------------
sh_files=("$SOURCE_DIR"/*.sh)
total=${#sh_files[@]}

if [ $total -eq 0 ] || [ ! -f "${sh_files[0]}" ]; then
    echo -e "${RED}❌ В папке нет .sh файлов${NC}"
    exit 1
fi

echo -e "${YELLOW}🔍 Найдено .sh файлов: $total${NC}"
echo ""

# -------------------- КОПИРОВАНИЕ И РЕГИСТРАЦИЯ --------------------
copied=0
failed=0

for script in "${sh_files[@]}"; do
    if [ -f "$script" ]; then
        filename=$(basename "$script")
        name="${filename%.sh}"
        
        echo -e "${BLUE}▶ Обрабатываю:${NC} $filename"
        
        # Копируем в .termux_scripts
        cp "$script" "$TARGET_DIR/$filename"
        if [ $? -eq 0 ]; then
            echo -e "   ${GREEN}✅ Скопирован в:${NC} $TARGET_DIR/$filename"
            
            # Даем права
            chmod +x "$TARGET_DIR/$filename"
            echo -e "   ${GREEN}✅ Права установлены${NC}"
            
            # Удаляем старый симлинк если есть
            rm -f "$BIN_DIR/$name" 2>/dev/null
            
            # Создаем новый симлинк
            ln -sf "$TARGET_DIR/$filename" "$BIN_DIR/$name"
            echo -e "   ${GREEN}✅ Симлинк создан:${NC} $BIN_DIR/$name"
            
            copied=$((copied + 1))
        else
            echo -e "   ${RED}❌ Ошибка копирования${NC}"
            failed=$((failed + 1))
        fi
        echo ""
    fi
done

# -------------------- НАСТРОЙКА PATH --------------------
if ! echo "$PATH" | grep -q "$BIN_DIR"; then
    echo 'export PATH="$PATH:$HOME/bin"' >> ~/.bashrc
    echo -e "${GREEN}✅ PATH настроен (добавлен ~/bin)${NC}"
fi

# -------------------- ПРИМЕНЯЕМ ИЗМЕНЕНИЯ --------------------
source ~/.bashrc

# -------------------- ИТОГ --------------------
echo -e "${BLUE}==============================================${NC}"
echo -e "${GREEN}✅ ОПЕРАЦИЯ ЗАВЕРШЕНА${NC}"
echo -e "${BLUE}==============================================${NC}"
echo ""
echo -e "${YELLOW}📊 Статистика:${NC}"
echo "   Всего файлов: $total"
echo "   Скопировано: $copied"
echo "   Ошибок: $failed"
echo ""
echo -e "${YELLOW}📋 Список доступных команд:${NC}"
ls -la "$BIN_DIR" | grep -E "мд|sh" | sed 's/^/   /'
echo ""
echo -e "${GREEN}🎯 Теперь все ваши команды работают из любой папки!${NC}"
echo ""
echo -e "${YELLOW}Пример проверки:${NC}"
echo "   which мдгтмл"
echo "   мдгтмл"
```

---

## 🚀 КАК ИСПОЛЬЗОВАТЬ:

### Шаг 1. Сохраните скрипт
```bash
nano /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/скопировать_все.sh
```

### Шаг 2. Вставьте код выше и сохраните (Ctrl+X → Y → Enter)

### Шаг 3. Сделайте исполняемым
```bash
chmod +x /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/скопировать_все.sh
```

### Шаг 4. ЗАПУСТИТЕ!
```bash
bash /storage/emulated/0/Documents/15_Настройки_скрипты_Termux_бекап_рестор/Алиасы_команды_SH/скопировать_все.sh
```

---

## ✅ ЧТО СДЕЛАЕТ СКРИПТ:

1. **Найдёт все** `.sh` файлы в вашей папке
2. **Скопирует** их в `~/.termux_scripts/`
3. **Даст права** на выполнение
4. **Создаст симлинки** в `~/bin/` для каждого файла
5. **Настроит PATH**, если нужно
6. **Покажет список** всех доступных команд

После этого все ваши команды (мдгтмл, мддок, мдпдф, мдепуб и т.д.) будут работать из любой папки!



