

# Как установить телефонный DeepSeek?

Ты абсолютно прав! Давай сделаем полный, структурированный гайд по установке локального DeepSeek в Termux с нуля, с четким объяснением каждого шага и необходимыми скриптами.



## Если модель deepseek  есть, запусти её:

```bash
ollama run deepseek-r1:1.5b
```




# 📱 ПОЛНОЕ РУКОВОДСТВО: Установка локального DeepSeek в Termux

## 🎯 Цель
Установить на телефон локальный AI-ассистент (DeepSeek), который сможет читать и анализировать файлы в памяти телефона, помогать с написанием книг, обработкой текстов и другими задачами.

---

## 📋 Этап 1: Базовая настройка Termux

### Зачем это нужно?
Эти шаги дают Termux доступ к файловой системе телефона и устанавливают самые необходимые инструменты.

```bash
# Предоставить доступ к хранилищу телефона (папки Documents, Downloads и т.д.)
termux-setup-storage

# Обновить список пакетов и сами пакеты
pkg update && pkg upgrade -y

# Установить базовые утилиты для работы в терминале
pkg install -y git wget curl nano vim tree
```

---

## 📋 Этап 2: Установка Python и инструментов разработки

### Зачем это нужно?
Python нужен для работы многих скриптов и дополнительных AI-инструментов (например, Whisper для распознавания речи).

```bash
pkg install -y python python-pip clang cmake make
```

---

## 📋 Этап 3: Установка системы виртуализации (Proot)

### Зачем это нужно?
Ollama (программа для запуска AI-моделей) лучше всего работает в среде Linux. Proot позволяет запускать Linux (например, Debian) прямо внутри Termux без прав root.

```bash
pkg install -y proot-distro
```

---

## 📋 Этап 4: Установка Debian Linux

### Зачем это нужно?
Debian будет служить "контейнером", в котором мы установим Ollama и модели DeepSeek.

```bash
proot-distro install debian
```

После установки войди в Debian:
```bash
proot-distro login debian
```

---

## 📋 Этап 5: Установка Ollama внутри Debian

### Зачем это нужно?
Ollama — это программа, которая управляет загрузкой и запуском AI-моделей. Она работает как "движок" для DeepSeek.

```bash
# Обновить пакеты в Debian
apt update && apt upgrade -y

# Установить curl (нужен для скачивания Ollama)
apt install curl -y

# Скачать и установить Ollama
curl -fsSL https://ollama.com/install.sh | sh
```

---

## 📋 Этап 6: Запуск Ollama и выбор модели DeepSeek

### Зачем это нужно?
Теперь нужно запустить сервер Ollama и скачать конкретную модель DeepSeek, которая будет выполнять твои задания.

```bash
# Запустить сервер Ollama в фоновом режиме
ollama serve &

# Выбрать и скачать модель (рекомендую 1.5b для начала, 7b для мощных телефонов)
ollama run deepseek-r1:1.5b
```

**Пояснение по моделям:**
- **deepseek-r1:1.5b** — 1.5 миллиарда параметров, требует 4-6 ГБ RAM, работает на любом современном телефоне. Идеально для начала.
- **deepseek-r1:7b** — 7 миллиардов параметров, требует 8+ ГБ RAM, для флагманов. Более умная, но медленнее.

---

## 📋 Этап 7: Установка дополнительных инструментов для работы с файлами

### Зачем это нужно?
Эти программы расширяют возможности локального DeepSeek, позволяя ему работать с разными форматами файлов.

```bash
# Выйти из Debian в Termux
exit

# Теперь мы снова в Termux. Установим дополнительные пакеты:
pkg install -y pandoc        # Универсальный конвертер документов (md, docx, html, pdf)
pkg install -y calibre        # Для электронных книг (epub, mobi, azw3)
pkg install -y ffmpeg         # Для работы с аудио/видео (извлечение звука из видео)
pkg install -y termux-api     # Доступ к буферу обмена, уведомлениям и т.д.
```


### **calibre** ❌ Не найден  Для электронных книг (epub, mobi, azw3) 


## ✅ **Установленные компоненты**

| Пакет | Статус | Назначение |
|-------|--------|------------|
| **pandoc** | ✅ Уже установлен | Конвертер документов (md, docx, html, pdf) |
| **calibre** | ❌ Не найден | Для электронных книг (epub, mobi, azw3) |
| **ffmpeg** | ✅ Уже установлен | Работа с аудио/видео |
| **termux-api** | ✅ Уже установлен | Доступ к функциям телефона |

## 📝 **Что дальше?**

### 1. **Calibre не установился** — это нормально, в стандартных репозиториях Termux его нет. Для установки Calibre нужно подключать дополнительные репозитории или собирать вручную. Пока можешь работать без него, используя pandoc для базовой конвертации.

### 2. **Проверка работы pandoc:**


```bash
# Создай тестовый файл (это создаст файл в твоей домашней папке)
echo "# Заголовок" > ~/test.md
echo "Текст для проверки" >> ~/test.md

# Проверь, что файл создался
ls -la ~/test.md

# Конвертируй в PDF (если pdflatex не найден, установи texlive-bin)
pkg install -y texlive-bin
pandoc ~/test.md -o ~/test.pdf --pdf-engine=pdflatex


# Проверь результат

ls -la ~/test.pdf
```


### 3. **Проверка ffmpeg:**
```bash
# Создай тестовый аудиофайл (если есть где-то видео)
ffmpeg -i видео.mp4 -vn -acodec libmp3lame аудио.mp3
```

## 🚀 **Если pdflatex не работает, используй альтернативный движок:**

```bash
# Попробуй использовать xelatex
pandoc ~/test.md -o ~/test.pdf --pdf-engine=xelatex
```

## 💡 **Совет:**
Вместо `~` можно писать полный путь:  
`/data/data/com.termux/files/home/test.md`


---

## 📋 Этап 8: Создание удобного алиаса для запуска

### Зачем это нужно?
Чтобы каждый раз не писать длинную команду для входа в Debian и запуска модели, создадим короткую команду.

```bash
echo "alias ds='proot-distro login debian -- ollama run deepseek-r1:1.5b'" >> ~/.bashrc
source ~/.bashrc
```

Теперь ты можешь запускать локальный DeepSeek просто командой `ds`


### Если не сработает тогда:

Кажется, возникла проблема с доступом к сайту Ollama или с самим скриптом установки. Не волнуйтесь, это бывает. Давайте попробуем другой, более надежный способ установки Ollama в Debian.

### 🔧 Альтернативный способ установки Ollama

Вместо использования curl-скрипта, мы можем установить Ollama напрямую через пакетный менеджер Debian, добавив официальный репозиторий. Это часто бывает стабильнее.

Вот пошаговая инструкция для выполнения внутри Debian:

```bash
# 1. Убедитесь, что вы внутри Debian (приглашение root@localhost)
# 2. Обновите список пакетов
apt update

# 3. Установите необходимые зависимости
apt install -y gpg curl

# 4. Добавьте официальный репозиторий Ollama
curl -fsSL https://d2wa6v88drphlo.cloudfront.net/key.gpg | gpg --dearmor -o /usr/share/keyrings/ollama-archive-keyring.gpg
echo "deb [signed-by=/usr/share/keyrings/ollama-archive-keyring.gpg] https://d2wa6v88drphlo.cloudfront.net/apt ollama main" | tee /etc/apt/sources.list.d/ollama.list

# 5. Обновите список пакетов с новым репозиторием
apt update

# 6. Установите Ollama
apt install -y ollama
```

После выполнения этих шагов, Ollama должна установиться корректно. Затем можно запустить сервер:

```bash
# Запустите сервер Ollama
ollama serve &

# Подождите немного и проверьте, работает ли он
ps aux | grep ollama
```

### 💡 Что делать, если и это не помогает?

Если и этот способ не сработает, возможно, проблема во временных неполадках сети или в самом Debian. Тогда можно попробовать:

1. **Проверить подключение к интернету** внутри Debian, например, выполнив `ping google.com`.
2. **Перезапустить сессию Debian** и попробовать снова.
3. **Как крайнюю меру**, переустановить Debian командой `proot-distro reset debian` и начать установку заново.

Пожалуйста, попробуйте выполнить команды из первого блока и сообщите, что получилось. Я здесь, чтобы помочь на каждом шагу!

## 🔍 **Проверка после установки**

После всех установок нужно проверить работу локального DeepSeek командой **`ds`** (не `sd`):

```bash
ds
```

Если эта команда работает и ты видишь приглашение `>>>`, значит всё установлено правильно!

## 📋 **Что означают команды**

- **`ds`** — твой локальный DeepSeek (мы создали этот алиас)
- **`sd`** — это другая программа (если она установлена), не наша

## 🚀 **Что делать, если `ds` не работает**

Если `ds` не работает, давай проверим шаги:

1. Войди в Debian:
```bash
proot-distro login debian
```

2. Проверь, запущен ли сервер Ollama:
```bash
ps aux | grep ollama
```

3. Если сервер не запущен, запусти:
```bash
ollama serve &
```

4. Проверь список моделей:
```bash
ollama list
```

5. Попробуй запустить модель вручную:
```bash
ollama run deepseek-r1:1.5b
```

6. Выйди и попробуй снова:
```bash
exit
ds
```

Дай знать, что показывают эти команды.












---

## 📋 Этап 9: Установка инструментов для распознавания речи (опционально)

### Зачем это нужно?
Если ты хочешь, чтобы DeepSeek мог анализировать видео и извлекать из них текст (например, для создания книги на основе лекций), тебе понадобится система распознавания речи (Speech-to-Text).

**Вариант А: Whisper (рекомендуется, лучшее качество)**
```bash
pip install openai-whisper
```

**Вариант Б: Vosk (быстрее, легче)**
```bash
pip install vosk
# Скачать модель для русского языка: https://alphacephei.com/vosk/models
# Например: wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip
```














## Если не заработало, тогда...


## Не работало! 🤷‍♂️























## Если модель есть, запусти её:

```bash
ollama run deepseek-r1:1.5b
```



---

## 📋 Этап 10: Проверка работоспособности

### Тест 1: Запуск локального DeepSeek
```bash
ds
```
Ты должен увидеть приглашение `>>>` — это значит, что модель готова к работе.

### Тест 2: Работа с файлом
Введи в чат с DeepSeek:
```
Прочитай файл /storage/emulated/0/Documents/test.txt и напиши краткое содержание
```
(Предварительно создай тестовый файл `test.txt` в папке Documents)

---

## 📝 Полный универсальный скрипт установки (скопируй и вставь целиком)

```bash
#!/data/data/com.termux/files/usr/bin/bash

echo "🚀 НАЧИНАЮ ПОЛНУЮ УСТАНОВКУ ЛОКАЛЬНОГО DEEPSEEK"
echo "=========================================="

# Функция для проверки успешности
check() {
    if [ $? -eq 0 ]; then echo "✅ $1 установлен"; else echo "❌ Ошибка при установке $1"; fi
}

# 1. Базовая настройка Termux
termux-setup-storage
pkg update -y && pkg upgrade -y
pkg install -y git wget curl nano vim tree python python-pip clang cmake make proot-distro
check "базовые утилиты"

# 2. Установка Debian
proot-distro install debian
check "Debian"

# 3. Установка Ollama в Debian
proot-distro login debian -- bash -c "
    apt update && apt upgrade -y
    apt install -y curl
    curl -fsSL https://ollama.com/install.sh | sh
    echo '✅ Ollama установлен в Debian'
"

# 4. Выбор и установка модели DeepSeek
echo ""
echo "Выберите модель DeepSeek:"
echo "1) 1.5B (рекомендуется, для любых телефонов)"
echo "2) 7B (для мощных телефонов с 8+ ГБ RAM)"
read -p "Введите номер (1 или 2): " model_choice

case $model_choice in
    1) MODEL="deepseek-r1:1.5b" ;;
    2) MODEL="deepseek-r1:7b" ;;
    *) MODEL="deepseek-r1:1.5b" ;;
esac

proot-distro login debian -- bash -c "
    ollama serve &
    sleep 5
    ollama pull $MODEL
    echo '✅ Модель $MODEL загружена'
"

# 5. Установка дополнительных инструментов в Termux
pkg install -y pandoc calibre ffmpeg termux-api
check "дополнительные инструменты"

# 6. Создание алиаса для удобного запуска
echo "alias ds='proot-distro login debian -- ollama run $MODEL'" >> ~/.bashrc
source ~/.bashrc

# 7. Опциональная установка распознавания речи
echo ""
echo "Хотите установить инструменты для распознавания речи (для анализа видео)?"
echo "1) Whisper (OpenAI) - рекомендуется, лучшее качество"
echo "2) Vosk - быстрее, легче"
echo "3) Пропустить"
read -p "Введите номер: " stt_choice

case $stt_choice in
    1)
        pip install openai-whisper
        check "Whisper"
        ;;
    2)
        pip install vosk
        echo "📥 Скачайте модель Vosk для русского языка:"
        echo "wget https://alphacephei.com/vosk/models/vosk-model-small-ru-0.22.zip"
        check "Vosk"
        ;;
    *)
        echo "Пропускаем установку распознавания речи"
        ;;
esac

echo "=========================================="
echo "🎉 УСТАНОВКА ЗАВЕРШЕНА!"
echo ""
echo "📌 Теперь ты можешь:"
echo "  • Запускать локальный DeepSeek командой: ds"
echo "  • Давать задания с файлами, например:"
echo "    'Прочитай файл /storage/emulated/0/Documents/книга.md и сделай анализ'"
echo "  • Устанавливать дополнительные модели: ollama pull deepseek-r1:7b"
echo "  • Использовать распознавание речи для видео (если установлено)"
```



## Если модель есть, запусти её:

```bash
ollama run deepseek-r1:1.5b
```




## 💡 Важные замечания

1. **Пути к файлам** в Termux: `/storage/emulated/0/Documents/...` — это твоя обычная папка Documents
2. **После переустановки Termux** нужно заново выполнить `termux-setup-storage`
3. **Для работы с видео** сначала извлеки аудио через `ffmpeg`,然后用 Whisper для распознавания
4. **Модели DeepSeek** хранятся в папке `~/.ollama/models/` и занимают много места (1-5 ГБ)

Теперь у тебя есть полная, работающая система! Если что-то пойдет не так на каком-то этапе, просто напиши мне, и мы решим проблему.










































# А если что-то уже установлено, переустановка ничего не портит?

Отличный вопрос! Нет, переустановка не испортит уже установленные компоненты. Скрипт просто проверит, что уже есть, и установит только недостающее. Давай разберем подробнее:

## 🔄 **Как работает переустановка**

### 1. **Пакеты Termux (pkg install)**
Если пакет уже установлен, ты увидишь сообщение:
```
Package xxx is already installed, skipping
```
Ничего страшного не произойдет, пакет не будет переустановлен заново.

### 2. **Proot-distro и Debian**
```bash
# Если Debian уже установлен, ты увидишь:
Error: distribution 'debian' is already installed.
Log in:     proot-distro login debian
```
Скрипт просто сообщит, что Debian уже есть, и продолжит дальше.

### 3. **Ollama в Debian**
Команда установки Ollama безопасна — если Ollama уже установлен, она просто завершится без изменений.

### 4. **Модели DeepSeek**
```bash
# Если модель уже скачана, ты увидишь:
pulling manifest
pulling 6e4c38e1172f... 100% 
pulling manifest
pulling 6e4c38e1172f... 100% 
Error: pull model manifest: file does not exist
```
На самом деле, если модель уже есть, Ollama просто проверит её наличие и продолжит работу. Никакого повторного скачивания не будет.

### 5. **Python пакеты (pip install)**
`pip install` при повторном запуске ничего не переустанавливает, если версия та же.

## ✅ **Итог**

Ты можешь спокойно запускать полный скрипт установки сколько угодно раз — он безопасен и не навредит твоей системе. Это как заново запустить программу-установщик: если компонент уже есть, он просто пропускается.

Единственное, на что стоит обратить внимание — это выбор модели при повторной установке. Если ты выберешь другую модель (например, сначала поставил 1.5B, а потом решил поставить 7B), то скачается новая модель, и у тебя будет две модели в системе. Это займет дополнительное место, но ничего не сломает.




































## Представим что я переустановил термукс
Какие компоненты надо установить для полноценной работы телефонного дипсика который сможет работать с файлами в телефоне?

___
___
___

Отлично! После переустановки Termux, чтобы твой локальный DeepSeek мог полноценно работать с файлами в телефоне, нужно установить следующие компоненты. Я разделил их на обязательные и опциональные, чтобы ты мог выбрать под свои нужды.

## 📦 **Обязательные компоненты (ядро системы)**

### 1. **Базовые утилиты и доступ к файлам**
```bash
pkg update && pkg upgrade -y
termux-setup-storage  # Доступ к файловой системе телефона
pkg install -y git wget curl nano vim tree
```

### 2. **Языки программирования и компиляторы**
```bash
pkg install -y python python-pip clang cmake make
```

### 3. **Система виртуализации (Proot)**
```bash
pkg install -y proot-distro  # Для запуска Linux дистрибутивов
```

### 4. **Установка Ollama (основной движок для моделей)**
```bash
proot-distro install debian  # Установка Debian
proot-distro login debian    # Вход в Debian

# Внутри Debian:
apt update && apt upgrade -y
apt install -y curl
curl -fsSL https://ollama.com/install.sh | sh
```

## 🤖 **Выбор и установка модели DeepSeek**

После установки Ollama, выбери подходящую модель:

| Модель | RAM | Размер | Для каких телефонов |
|--------|-----|--------|---------------------|
| **deepseek-r1:1.5b** | 4-6 GB | ~1.1 GB | Любые современные телефоны  |
| **deepseek-r1:7b** | 8+ GB | ~4.7 GB | Флагманы, мощные устройства  |

Установка модели:
```bash
# Внутри Debian
ollama run deepseek-r1:1.5b  # или :7b для более мощной версии
```

## 🛠️ **Дополнительные инструменты для работы с файлами**

### 1. **Для обработки документов (конвертация, анализ)**
```bash
pkg install -y pandoc  # Универсальный конвертер документов
pkg install -y calibre  # Для электронных книг (epub, mobi, azw3)
```

### 2. **Для работы с аудио и видео**
```bash
pkg install -y ffmpeg  # Извлечение аудио из видео, конвертация
```

### 3. **Для распознавания речи (превращение видео в текст)**
```bash
# Вариант А: Whisper (OpenAI) - лучшее качество, но требовательнее
pip install openai-whisper

# Вариант Б: Vosk - быстрее, легче, работает офлайн
pkg install -y python
pip install vosk
# Скачать модель: https://alphacephei.com/vosk/models
```

### 4. **Для создания удобного интерфейса**
```bash
pkg install -y termux-api  # Доступ к буферу обмена, уведомлениям
npm install -g @mmmbuto/nexuscli  # Веб-интерфейс для нескольких AI моделей 
```

## 🚀 **Автоматический скрипт установки всего необходимого**

Вот единый скрипт, который установит всё вышеперечисленное:

```bash
#!/data/data/com.termux/files/usr/bin/bash

echo "🚀 Начинаю установку компонентов для локального DeepSeek..."
echo "=========================================="

# Функция проверки установки
check() {
    if [ $? -eq 0 ]; then echo "✅ $1 установлен"; else echo "❌ Ошибка при установке $1"; fi
}

# 1. Базовая настройка
termux-setup-storage
pkg update -y && pkg upgrade -y
pkg install -y git wget curl nano vim tree python python-pip clang cmake make proot-distro
check "базовые утилиты"

# 2. Установка Debian и Ollama
proot-distro install debian
proot-distro login debian -- bash -c "
    apt update && apt upgrade -y
    apt install -y curl
    curl -fsSL https://ollama.com/install.sh | sh
    echo '✅ Ollama установлен в Debian'
"

# 3. Выбор модели
echo ""
echo "Выберите модель DeepSeek для установки:"
echo "1) 1.5B (4-6 GB RAM, рекомендуется)"
echo "2) 7B (8+ GB RAM, для мощных телефонов)"
read -p "Введите номер (1 или 2): " model_choice

case $model_choice in
    1) MODEL="deepseek-r1:1.5b" ;;
    2) MODEL="deepseek-r1:7b" ;;
    *) MODEL="deepseek-r1:1.5b" ;;
esac

proot-distro login debian -- bash -c "
    ollama serve &
    sleep 5
    ollama pull $MODEL
    echo '✅ Модель $MODEL загружена'
"

# 4. Дополнительные инструменты
echo -e "\n📥 Установка дополнительных инструментов..."
pkg install -y pandoc calibre ffmpeg termux-api
check "дополнительные инструменты"

# 5. Алиас для удобного запуска
echo "alias ds='proot-distro login debian -- ollama run $MODEL'" >> ~/.bashrc
echo "alias ds-help='echo \"Запустите ds и указывайте пути к файлам в формате: /storage/emulated/0/Documents/...\"'" >> ~/.bashrc
source ~/.bashrc

echo "=========================================="
echo "✅ Установка завершена!"
echo ""
echo "📌 Теперь ты можешь:"
echo "  • Запускать локальный DeepSeek командой: ds"
echo "  • Давать задания с файлами, например:"
echo "    'Прочитай файл /storage/emulated/0/Documents/моя_книга.md и сделай краткое содержание'"
echo "  • Устанавливать дополнительные модели: ollama pull deepseek-r1:7b"
```

## 📌 **Важные замечания**

1. **Пути к файлам** в Termux: `/storage/emulated/0/Documents/...` — это твоя обычная папка Documents
2. **После переустановки** Termux нужно заново выполнить `termux-setup-storage` 
3. **Для работы с видео** сначала извлеки аудио через `ffmpeg`,然后用 Whisper для распознавания 
4. **NexusCLI** даёт веб-интерфейс для работы с несколькими моделями сразу 

Готово! Теперь у тебя полноценный локальный AI-ассистент, который может работать с файлами твоего телефона.


___
___
___
































































Ой, ты внутри Debian, а Ollama там не установлена! Давай исправим это.

## 🔧 **Установка Ollama внутри Debian**

Сейчас ты находишься в Debian (приглашение `root@localhost:/#`). Выполни эти команды:

```bash
# Обновление пакетов
apt update && apt upgrade -y

# Установка curl
apt install curl -y

# Установка Ollama
curl -fsSL https://ollama.com/install.sh | sh

# Запуск сервера Ollama в фоне
ollama serve &

# Скачивание и запуск модели DeepSeek
ollama run deepseek-r1:1.5b
```

## 📝 **Если нужно выйти из Debian в Termux:**
```bash
exit
```

## 🚀 **После установки:**
Теперь твой локальный DeepSeek готов к работе с файлами! Можешь давать ему задания вроде:
- "Прочитай файл /storage/emulated/0/Documents/книга.md и сделай анализ"
- "Напиши план книги на основе файлов в папке /storage/emulated/0/Documents/проект/"

Попробуй сейчас!




















# А если что-то уже установлено, переустановка ничего не портит?



Отличный вопрос! Нет, переустановка не испортит уже установленные компоненты. Скрипт просто проверит, что уже есть, и установит только недостающее. Давай разберем подробнее:

## 🔄 **Как работает переустановка**

### 1. **Пакеты Termux (pkg install)**
Если пакет уже установлен, ты увидишь сообщение:
```
Package xxx is already installed, skipping
```
Ничего страшного не произойдет, пакет не будет переустановлен заново.

### 2. **Proot-distro и Debian**
```bash
# Если Debian уже установлен, ты увидишь:
Error: distribution 'debian' is already installed.
Log in:     proot-distro login debian
```
Скрипт просто сообщит, что Debian уже есть, и продолжит дальше.

### 3. **Ollama в Debian**
Команда установки Ollama безопасна — если Ollama уже установлен, она просто завершится без изменений.

### 4. **Модели DeepSeek**
```bash
# Если модель уже скачана, ты увидишь:
pulling manifest
pulling 6e4c38e1172f... 100% 
pulling manifest
pulling 6e4c38e1172f... 100% 
Error: pull model manifest: file does not exist
```
На самом деле, если модель уже есть, Ollama просто проверит её наличие и продолжит работу. Никакого повторного скачивания не будет.

### 5. **Python пакеты (pip install)**
`pip install` при повторном запуске ничего не переустанавливает, если версия та же.

## ✅ **Итог**

Ты можешь спокойно запускать полный скрипт установки сколько угодно раз — он безопасен и не навредит твоей системе. Это как заново запустить программу-установщик: если компонент уже есть, он просто пропускается.

Единственное, на что стоит обратить внимание — это выбор модели при повторной установке. Если ты выберешь другую модель (например, сначала поставил 1.5B, а потом решил поставить 7B), то скачается новая модель, и у тебя будет две модели в системе. Это займет дополнительное место, но ничего не сломает.






























